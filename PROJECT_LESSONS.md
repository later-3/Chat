# Chat 项目经验与反例

## 1. 文档用途

本文记录已经发生、可能再次发生，并会实质性伤害产品或工程判断的错误。

它不是复盘归档，而是项目的**强制决策约束**：任何参与者在回复项目问题、编写方案、修改文档或代码之前，都必须先阅读本文；发生上下文压缩、切换任务或收到用户纠正后，必须重新阅读。

新增经验时，应写清：错误触发、错误本质、可能后果、正确规则和回复前检查项。不能只写“注意”“避免”或情绪性结论。

## 2. 每次回复前检查

1. **产品身份**：是否把 Chat 作为独立、完整、持续运营的产品来思考，而不是某个外部系统的附属实现、适配器或玩具？
2. **目标与交付**：是否先覆盖完整用户场景和目标能力，再把实现拆成阶段？是否误用当前进度、团队规模或已有代码缩小目标架构？
3. **证据与推导**：事实、参考项目经验、本项目推导和待审核决定是否明确分开？
4. **可执行性**：架构师、项目经理、开发和产品负责人能否分别从文档中找到自己需要的信息？
5. **场景穿透**：场景是否具体落到模块、组件、合同、状态变化、失败分支、恢复方式和用户可见结果？
6. **范围措辞**：是否出现“初期、第一版、早期、当前规模”等词来为目标能力缺失辩护？如果是，立即重写。交付阶段只能描述能力的启用顺序，不能重新定义项目边界。
7. **参考可追溯性**：架构和模块是否能逐项追溯到参考项目源码事实或本项目明确需求？是否先发明“平面、中心、层”等抽象，再让真实模块勉强归类？如果是，立即回到源码结构重新推导。
8. **入口拓扑**：是否把最终用户使用的聊天平台、协议Adapter、产品Web客户端、受信后端入口和产品核心画成了同一级“入口”？每一条入站箭头是否说明协议终止点、认证位置、适配责任和能够调用的后端合同？没有说明则不得提交架构。
9. **审批完整性**：模型调用审批是否覆盖真正发送给Provider的完整请求，而不是只允许修改Prompt？两个审核视图、编辑器、Hash和实际HTTP Body是否来自同一份规范请求？任一项不一致就不能发送。
10. **审批可读性**：所谓“可读视图”是否仍然要求用户读写JSON？Provider和模型是否允许自由填写？可读视图必须使用固定、不可改名的Key和符合值类型的控件；Provider与模型必须来自服务端能力目录并联动选择。原始JSON只属于高级核对视图。
11. **对象可理解性**：是否只画了模块和箭头，却没有告诉读者一次请求中会出现哪些对象、对象由谁创建、保存在哪里、何时结束以及如何相互引用？是否把Agent写成一个无法打开的黑盒？如果是，先补对象词典、Agent内部组成和端到端对象流，再提交架构审核。
12. **操作可走通性**：是否虽然列出了对象，却仍没有从用户一次具体点击开始，逐步讲清前端函数、协议入口、后端协调、数据库读写、Agent的Session/Tool分工、Provider请求、响应解析、产品提交和前端渲染？如果读者不能顺着“点击→模型→解析→呈现”复述完整链路，说明文档仍是静态名词表，不是可理解的运行架构。
13. **Workflow选择与运行**：是否在用户发送Prompt之前明确选择并绑定Workflow，还是擅自让系统根据输入自动决定？当前必须由用户选择Workflow；未来Agent只能提出建议，未经用户接受不能替换本次选择。Workflow Run View展示的是已选Workflow的实际Run，不是选择入口或独立运行器。
14. **布局隐喻与产品对象**：是否因为用户用ChatGPT Canvas举例说明左右分栏，就把通用右侧工作区、Workflow运行视图和未来白板Canvas混成同一个“Canvas”？布局容器只负责承载视图；Workflow Run View投影执行；Canvas保留给可持久化、可编辑的空间型Artifact。
15. **概念空间**：是否使用了`Session`、`Run`、`Workflow`、`Context`、`Agent`、`Tool`、`Approval`或`Canvas`等裸称，却没有判断产品、协议、运行时和界面中的具体对象？是否把概念状态、实现状态和项目事实写进同一份大词典？如果是，先进入概念索引恢复唯一语义边界，再更新真正的事实所有者。
16. **Workflow审批策略**：是否把Workflow选择、整轮运行授权和逐次模型调用审批混成同一种“确认”？每个可执行Workflow必须在发送前公开自己的审批模式和预计中断次数；一次授权不能冒充已经逐字审核尚未生成的后续Provider请求。
17. **回合沉淀与工作连续性**：是否只设计了输入、执行和回复，却漏掉每次Interaction结束后的主题摘取、原始事实保存、记忆候选、项目/Work状态更新和下一轮检索索引？“不更新项目长期记忆”不能被写成“不留下任何记忆”；至少要保存原始Message，并形成有来源、可失效的回合摘要候选。项目识别必须先用轻量索引召回候选，再在确认目标后装配该项目的完整ContextPackage。
18. **可配置HITL与Prompt编译**：是否把HITL实现成每个节点固定弹窗，或把User Prompt加历史记录直接当成Agent执行Prompt？每个决策点必须有显式、分作用域的暂停策略；自动通过仍要记录规则、版本和后果。Agent收到的应是由Intent、Project/Work、ContextPackage、TaskPlan、权限和验证合同编译出的最小充分RunSpec，而不是无边界历史拼接。
19. **澄清可回答性**：界面是在让用户审核一个候选，还是要求用户补充新信息？审批只能提供接受、修改、跳过或停止；澄清必须提供文字/选项回答入口，或结束当前Interaction并把问题作为Assistant Message交回聊天输入。明确的查询不能被模型擅自扩大成“查询还是新建”。
20. **Python环境隔离**：开发、迁移、测试和真实模型验证是否全部使用仓库约定的`uv`项目虚拟环境？不得因为虚拟环境损坏或宿主原生扩展异常而静默回退到系统Python；应重建项目环境，并把仍存在的宿主故障与代码失败分开报告。
21. **产品级工程收敛**：重大纵向能力完成后，是否重新审视模块职责、依赖、错误合同、日志、指标、调试、注释、测试覆盖、CI和文档一致性？测试数量和一次通过不能代替这项审计。
22. **可持续模块质量**：本次能力是否继续堆进大型Service、Hook或组件？拆分是否依据状态所有权、事务边界和变化原因，而不是机械按行数切文件？关键不变量、恢复原因和状态转换是否有有效注释与结构化日志，并且存在防止边界回退的自动测试？
23. **外部编码Agent权限**：是否把Kimi等CLI的非交互模式误当成受治理执行？开发审查默认使用工具级只读Agent；自动批准的Print模式不得修改代码，产品接入必须用可中断协议并诚实说明Provider Payload可见性。
24. **Workflow路由可解释性**：运行视图是否只把Definition节点平铺，迫使用户猜实际路径？选择节点必须显示候选边、声明顺序、公开条件、实际值、选中目标与未走原因；旧Trace缺少逐项求值时必须明确降级，不能拿当前代码推断冒充历史事实。
25. **协作协议与步骤投影**：是否把项目方法、学习方法、用户质量标准继续散落在Prompt、Memory和Workflow条件中？是否把整份RunSpec、全部History或全部Harness状态交给每个Agent？协作方法必须有版本和绑定，执行层只接收当前职责所需的最小工作包与权限。
26. **Chat Harness边界**：是否把Chat Harness误写成大Prompt、MAF包装器或前端信息面板？Harness必须拥有产品事实、协作协议、Context选择、治理、结果沉淀和三方投影；MAF只拥有Agent/Workflow运行语义，前端只展示和提交用户动作，执行层只能按RunSpec工作。
27. **移动端产品完整性**：是否把“手机可用”缩成网络可达、响应式聊天框或一个移动视口测试？移动端必须覆盖Chat Web已经承诺的完整工作空间，包括连续对话、主页/时间导航、文件与Artifact、Workflow Run、Project/Work进度、Harness信息、HITL、配置、恢复和可观察状态；功能可以按手机信息层级重排，但不能静默缺失。
28. **超级管理员运营看护**：是否把“执行可看护”或个人主页误当成超级管理员看护？独立运营的Chat必须让超级管理员基于可信身份和权威产品事实查看谁登录、怎样使用、Project/Work/Artifact推进到哪里；登录时长、前台活跃时长、有效协作活动和运行耗时必须分开，管理查询必须鉴权、审计且不能建立第二套进度事实。
29. **执行层经验必须有案例**：是否只把“Hash要完整”、“必须fail closed”一类抽象口号交给Kimi/pi，却没有实际错误、后果、正例、适用条件和验证方法？可复用经验必须绑定真实案例与证据，并区分硬不变量、情境建议和执行层自主空间；只按当前任务选取相关经验，不把全部教训无界堆入Prompt。
30. **pi安装与运行身份**：是否把“同一份pi安装文件”误写成“共享同一配置、Session和权限”，或只检查全局pi配置就断言Chat执行层没有配置Provider？必须同时核对CLI realpath/inode与版本、Chat隔离`PI_CODING_AGENT_DIR`、Tool Profile Revision、临时模型目录、Gateway路由和真实ToolExecution；配置的合同版本还必须与实际安装版一致。
31. **测试必须独立收集**：数据库测试是否依赖另一个测试文件先导入SQLAlchemy模型，才让`Base.metadata`完整？每个测试模块或共享Fixture必须显式注册自身依赖的模型；单文件、随机顺序和全量收集都应得到同一Schema与结果。
32. **沙箱中的虚拟环境路径**：是否把`.venv/bin/python`启动器和它解析到的uv解释器目录混成一个路径，导致解释器无法读取运行时，或为了修复而开放整个用户目录？编译阶段保留虚拟环境启动器语义，沙箱只额外放行精确解析出的解释器发行目录和必要系统运行时。
33. **外部产品研究证据**：是否只根据官网、README、功能清单、Star或零散评论就判断某个产品“最接近”Chat？深层研究必须固定源码版本，还原核心对象和真实调用链，区分文档宣称、代码存在、运行实测、用户效果与口碑样本；无法取得源码或产品实例时必须明确证据上限。
34. **验证产物收口**：是否把一次冒烟验证留下的脏Worktree、测试Product Session或其他临时数据称为“证据”，长期占用正式项目和产品数据？有长期价值的证据必须进入测试、Trace、Artifact或正式文档并被项目使用；没有长期价值的验证产物应由创建者按权威生命周期立即清理，不能把是否清理推给用户。
35. **懒加载样式所有权**：同步渲染的App Shell、全局导航或共享组件是否错误依赖某个懒加载页面才引入的CSS？必须用“直接恢复非默认视图后整页刷新”的浏览器场景验证样式、尺寸和横向溢出，而不能只从默认页面进入后再切换。
36. **链路可学习性与双Trace**：是否只给少数大文件加了概览Docstring，就宣称整条Workflow已经可掌握？持续协作链的关键类、函数和跨模块调用必须就地标明阶段、节点、输入、输出、状态所有者、选择/为空原因及下游；每个终态Run还必须从同一组结构化事实确定性生成机器诊断Trace和人类流程报告，不能让Agent事后编故事，也不能把隐藏推理写进Trace。
37. **跨仓源码调试所有权**：是否把Chat工作区能解析pi的Source Map误当成用户可以在pi项目中正常下断点？Chat窗口只负责启动带Inspector的pi子进程；pi源码窗口必须拥有编译、断点和Attach配置。跨仓`outFiles`不能代替独立源码工作区。
38. **学习文档必须由代码事实生成口径**：是否在不同文档手写“6阶段、7阶段、12阶段”却没有说明它们分别是产品交付阶段、Workflow学习分组还是代码执行阶段？主Workflow的Definition版本、节点、边、学习阶段ID和节点归属必须从当前Catalog/Graph Factory提取并自动比对；任何教学性别名都要显式映射真实ID。架构、模块、基础概念、例子、代码锚点和实验缺少任一项时必须标为待补，不能用一篇总览或节点清单冒充教材已经完整。

## 3. 反例 001：把交付阶段偷换成目标架构范围

### 错误触发

架构文档反复使用“初期”“第一版”“早期”“当前规模”，并依据当前只有 Hello World、单用户或本地运行来决定目标架构需要考虑什么。

### 错误本质

把“先实现什么”偷换成“系统最终只需要什么”。交付阶段属于计划问题；目标架构属于完整用户场景、产品责任和质量保证问题。两者有依赖关系，但不能相互替代。

### 可能后果

1. 重要能力没有进入目标模型，后续只能用破坏性重构补救。
2. 数据、ID、状态机和恢复语义从一开始就无法承载完整产品。
3. 项目经理看不到完整工作包和依赖，只能走一步算一步。
4. 开发会把临时实现当作正式边界，形成双状态、双协议和不可恢复执行。
5. 项目被错误定位成短期演示，而不是持续运营的独立产品。

### 正确规则

1. 先从完整用户场景推导目标能力、质量保证、模块、状态所有权和技术合同。
2. 目标架构必须描述系统完整形态，即使某些能力尚未进入当前交付。
3. 阶段划分放在架构文档最后，只表达实现顺序、依赖和阶段验收。
4. 某阶段尚未实现的目标能力必须仍在架构中有明确位置、边界和演进合同。
5. 当前工程事实只能影响排期、验证方法和迁移方案，不能缩小产品身份或目标场景。

### 强制检查

- 架构正文是否在阶段计划之前已经完整回答：用户场景、系统保证、模块组成、交互、状态、接口、失败恢复和部署拓扑？
- 每个交付阶段是否能映射回既有目标模块，而没有临时创造另一套边界？
- 删除阶段章节后，架构文档是否仍然完整、可实施？如果不能，说明阶段仍在替代架构。

## 4. 反例 002：把外部集成角色写成产品从属身份

### 错误触发

因为 Chat 可以作为一个交互入口接入 OPC-OS Chat，就把本项目定义成“只是一个自研通道”“不是完整上位系统”。

### 错误本质

混淆了两个不同维度：

- **产品身份**：Chat 是独立开发、独立运行、独立运营和持续演进的完整产品。
- **集成角色**：在某个外部拓扑中，Chat 可以通过合同提供或消费通道能力。

外部拓扑中的角色不能反向削弱产品边界。“不是什么上位系统”也没有提供可实施的边界，只会制造从属暗示。

### 可能后果

1. 对话、工作、执行、知识、证据、交付、治理和运维责任被错误删减。
2. 架构围绕适配层而不是用户闭环设计。
3. 团队误以为外部系统会替本项目承担事实源、恢复或权限责任。
4. 产品在文档和实现上被降级成演示入口或薄壳。

### 正确规则

1. 不依赖 OPC-OS 的一句话，必须也能完整定义 Chat 的用户、价值、边界和责任。
2. OPC-OS Chat 只出现在“外部集成关系、合同和场景”中，不作为 Chat 产品身份的上位层级。
3. 集成不能形成第二个事实源；状态归属、版本、幂等、权限和证据传播必须通过明确合同处理。
4. 禁止再用“只是、仅仅、附属、非完整上位系统”等措辞描述本项目。

### 强制检查

- 产品定义能否独立成立并覆盖完整用户闭环？
- 外部集成是否画成对等系统间合同，而不是父子层级？
- 文档是否明确本项目自己承担的产品事实、执行治理和运营责任？

## 5. 反例 003：用模块名字清单冒充架构设计

### 错误触发

列出若干模块名称，再用“分层、解耦、可扩展”解释合理性，却没有说明模块内部组成、合同、状态和协作方式。

### 错误本质

分类不等于设计。一个能指导开发的模块定义，至少必须回答：

1. 用户价值与存在原因。
2. 负责和明确不负责的内容。
3. 内部组件及各自职责。
4. 拥有的领域对象、状态和存储。
5. 入站命令、查询或事件。
6. 出站合同、领域事件和依赖。
7. 依赖方向与禁止依赖。
8. 核心不变量、权限和并发规则。
9. 失败、重试、恢复、补偿和人工介入方式。
10. 技术落点、可观测性、测试和验收标准。

### 正确规则

总体架构中的每个模块都必须按上述内容展开。尚未审核的字段级 Schema 可以后置，但对象边界、合同语义、状态所有权和失败保证不能省略。

## 6. 反例 004：用户场景没有穿透到架构

### 错误触发

场景只写“用户发消息—系统理解—Agent 执行—返回结果”，没有对应到具体模块、状态和失败处理。

### 错误本质

场景变成产品口号，无法证明架构真的能满足用户。完整的架构场景至少必须包含：

1. 参与者、前置状态和触发动作。
2. 按顺序经过的前端组件、后端模块、运行时和外部系统。
3. 每一步使用的命令、查询、事件或版本化合同。
4. 权威状态在哪里、如何变化、何时对用户可见。
5. 审核、权限、幂等和并发控制点。
6. 断线、崩溃、超时、重复请求、结果未知等失败分支。
7. 恢复、对账、补偿或人工处置路径。
8. 最终用户看到什么，以及哪些证据证明任务真的完成。

### 正确规则

每个核心用户场景都必须给出“场景步骤—模块/组件—合同/状态—失败与恢复—用户结果”的对应关系，并反向检查每个目标模块至少服务一个明确场景。

## 7. 文档质量验收

一份总体架构文档只有同时满足以下 4 类读者才可提交审核：

1. **架构师**：能据此继续做组件、数据、接口、部署和非功能方案，不需要猜边界。
2. **项目经理**：能看到完整工作包、依赖、风险、阶段顺序和验收结果。
3. **开发**：能知道代码放在哪里、模块如何交互、谁拥有状态、失败怎么处理、应该测什么。
4. **产品负责人**：能知道为什么这样设计、证据来自哪里、它如何覆盖真实用户场景，以及哪些仍待决定。

不满足任一类读者，就不能用“这是总体架构，详细设计以后再说”掩盖缺失；总体架构可以不冻结字段级实现，但必须给出足以继续设计和排期的边界与保证。

## 8. 反例 005：用自创抽象替代参考项目的真实运行结构

### 错误触发

已经有pi、nanobot、LibreChat等源码参考，却没有先还原它们真实的模块、调用链、状态所有者和恢复边界，而是直接发明“数据平面、知识平面、控制平面”等分类，再把产品对象塞进这些分类。

### 错误本质

抽象层级走反了。架构术语只有在压缩了一组已经讲清楚的真实模块和共同约束时才有价值；如果术语本身不能回答类、服务、存储、入口、输出和失败责任，它就不是架构，只是重新命名。

这种写法还切断了证据链：读者无法判断某个模块来自参考项目的成熟经验、本项目用户需求，还是作者个人偏好。

### 可能后果

1. 架构师无法继续拆组件、接口和部署方案，因为抽象名下没有明确运行单元。
2. 开发不知道代码放在哪里、调用谁、状态由谁提交以及失败后谁恢复。
3. 项目经理无法从模块得到工作包和依赖顺序。
4. 产品负责人无法审核为什么需要这些模块，也无法判断参考项目是否真的支持结论。
5. 后续详细设计会各自重新解释抽象词，形成重复模块、双状态源和冲突协议。

### 正确规则

1. 先固定参考项目版本，并从源码还原每个项目的组合根、入口、核心协调器、适配器、状态存储和关键调用链。
2. 再建立“参考事实 -> 解决的工程问题 -> 本项目是否有同类问题 -> 采用、改造或拒绝 -> 本项目模块”的逐项推导。
3. 本项目模块使用能够直接映射到代码责任的名称，例如Conversation、Message、Run、Agent Runtime、Tool、Memory、Channel、Delivery，而不是用抽象平面代替它们。
4. 每个模块必须写清内部组件、状态所有者、入站和出站合同、依赖方向、失败恢复和场景，不允许只画方框。
5. 参考项目没有覆盖Intent、Work、Approval、Evidence等需求时，必须明确标为“本项目需求推导”，不得伪装成参考项目经验。
6. “层、平面、中心”等词只能在真实模块全部定义之后用于总结共同部署或依赖规律，不能成为模块本身。

### 强制检查

- 删除所有“平面、中心、层”标题后，文档是否仍能从真实模块和调用关系完整说明系统？
- 每个本项目模块是否至少有一条源码参考证据或一条明确的本项目用户需求依据？
- 是否能够对每个模块回答“为什么不是合并进相邻模块”和“为什么不是参考项目原样复制”？
- 是否有一张完整映射表，展示参考项目模块如何进入本项目的采用、改造或拒绝判断？

## 9. 反例 006：把聊天平台直接连到产品后端

### 错误触发

架构图已经有与后端绑定的Chat Web，却又把“OPC-OS Chat / Telegram / 其他聊天入口”画成可以直接进入同一个产品核心的并列入口，没有画出Channel Adapter、Bot/Webhook接入服务、协议转换、认证和受信调用边界。

### 错误本质

把5个不同角色混成“入口”：

1. 最终用户使用的聊天平台，例如Telegram。
2. 平台Bot、Webhook或长连接协议端点。
3. 把平台协议转换成内部合同的Channel Adapter/Gateway。
4. Chat自带Web客户端和它专用的产品API/AG-UI后端。
5. 真正拥有Conversation、Interaction和Run的产品核心。

聊天平台本身通常不能、也不应该直接调用产品内部模块。只有部署在受信边界内的Adapter/Gateway才能完成来源验证、身份映射、幂等接纳和协议转换后，调用公开的入站应用合同。Chat Web则是本产品自带客户端，通过自己的后端接口访问同一产品核心，不应被误写成一个通用Channel Adapter。

### 为什么危险

1. 开发无法判断Telegram Bot、Webhook和Chat后端分别部署在哪里。
2. 外部平台ID可能被错误地当成Product Session或授权凭据。
3. Web专用AG-UI/REST合同可能被外部Channel错误复用，反过来污染前端协议设计。
4. OPC-OS Chat等外部系统与Telegram等终端平台被错误地视为同一种对象。
5. 无法定义协议终止、重试、回执、能力降级和故障隔离责任。

### 强制规则

1. 架构图必须分别画出`Chat Web -> Chat Backend`和`External Platform -> Channel Adapter/Gateway -> Chat Ingress Contract -> Product Core`。
2. 外部平台不得直接指向Conversation、Interaction协调器、Run或数据库。
3. 每个Channel必须明确：协议端点、来源验证、身份/Session Binding、入站幂等、能力转换、出站Delivery和回执。
4. OPC-OS Chat、Telegram和其他平台是否属于同一种集成角色，必须先依据其真实架构和合同判断，不能用一个斜杠标签合并。
5. 新增相似产品参考时，先还原其Channel/UI/Core真实拓扑，再决定Chat采用直连、内置Adapter、独立Gateway或上游聚合器；不得先画目标图再找参考解释。

### 回复前检查

- 图中每条跨系统箭头的发起者真能调用接收方合同吗？
- Chat Web是否明确是产品自带客户端，而不是“众多外部Channel之一”？
- Telegram等平台与产品后端之间是否存在可部署、可认证、可测试的Adapter？
- Adapter失败是否与Chat产品核心故障区分？
- 外部系统和Chat分别拥有哪些事实，是否会双写？

## 10. 反例 007：只让用户修改Prompt，却把其他模型请求内容锁死

### 错误触发

模型调用审批界面展示Instructions、完整消息、知识内容、Tool定义和模型参数，但“修改”操作只允许改变最后一段用户Prompt，或者两个审核视图分别拼装数据。

### 错误本质

用户批准的是一次完整Provider请求，不是一段孤立文本。任何会进入Provider请求Body的内容都可能改变模型行为；如果它可见却不可修改，或展示对象与发送对象不是同一份数据，审批就不能证明“用户看见并批准的内容等于实际发送内容”。

### 可能后果

1. Instructions、历史、知识或Tool定义错误时，用户只能放弃整轮，无法纠正。
2. 修改Prompt后旧审批仍可能错误地绑定其他已变化字段。
3. 可读视图、JSON视图和实际HTTP Body发生漂移，形成虚假审核。
4. 自动Tool循环或Provider SDK二次改写请求，绕过每次模型调用审批。
5. 放弃后输入被清空，用户需要重新输入，控制感和可恢复性下降。

### 正确规则

1. 每次模型调用建立独立、版本化的`ModelCallDraft`；一个Product Run可包含多次模型调用和多份草稿。
2. 所有实际进入Provider请求Body的字段均可在草稿中修改；内部ID、Hash、鉴权Header和运行控制字段不冒充模型请求内容。
3. 可读视图、原始JSON、请求Hash和发送字节必须由同一份规范Provider请求投影。
4. 任意修改产生新版本和新Hash，旧Draft与Approval立即失效，必须再次审批。
5. 可编辑不等于任意组合可发送；格式、Provider能力、工具授权和已批准产品策略必须在发送前校验。
6. MAF自动Tool循环必须关闭；工具结果导致的下一次模型调用必须重新生成草稿并再次审批。
7. 放弃当前调用时不得产生Provider Attempt；原用户输入返回编辑框，只有显式点击清空按钮才删除。

### 强制检查

- 修改Instructions、任一历史消息、知识内容、Tool Schema、模型和输出参数时，是否都产生新版本与新Hash？
- Provider抓包Body是否与批准字节逐字节相同？
- 第二次及后续模型调用是否仍会进入审批节点？
- 拒绝或放弃后Attempt是否为0，输入是否仍可编辑和重新发送？

## 11. 反例 008：把JSON文本框包装成“可读编辑”

### 错误触发

审批页虽然区分“可读编辑”和“Provider JSON”，但可读视图里的完整消息、Tools和模型参数仍是JSON文本框；模型也允许用户自由输入，而不是从Provider支持的模型中选择。

### 错误本质

JSON是传输结构，不是面向普通用户的交互结构。两个视图同源解决的是一致性问题，不能替代信息架构和输入约束。Provider、模型和参数之间还存在能力关系；自由填写会让用户在批准之后才收到Provider错误。

### 可能后果

1. 用户需要理解括号、引号、数组和转义，审批理解成本没有真正降低。
2. 用户可能误改Key或破坏结构，而不是只修正业务值。
3. Provider与模型不匹配，审批后的请求仍必然失败。
4. Reasoning、输出和传输参数没有按布尔、数值、枚举等语义提供约束。
5. “可读视图”和“高级JSON视图”只有标题不同，产品失去分层价值。

### 正确规则

1. Provider和模型由服务端返回能力目录；前端先选Provider，再只显示该Provider允许的模型。
2. 可读视图中的Key固定展示、不可改名；Value按类型使用文本框、文本域、下拉框、数字框或开关。
3. 对象和数组拆成有标题的卡片与层级，不显示JSON语法；消息至少分Role、Content和内容片段。
4. 可选参数只能从已知参数目录添加，不提供任意Key输入；需要编辑未知扩展字段时进入Provider JSON高级视图。
5. 两种视图仍投影同一份草稿；任一友好控件修改后，Provider JSON、版本、Hash和发送Body同步变化。

### 强制检查

- 不打开Provider JSON时，用户能否完成常用字段的查看和修改？
- 可读视图中是否还存在要求用户直接编辑JSON的textarea？
- Provider切换后，模型选项和参数能力是否同步收窄？
- 固定Key是否不可改名，而Value仍能按类型修改？

## 12. 反例 009：模块边界完整，但读者仍不知道系统里面有什么

### 错误触发

架构文档已经列出Web、Conversation、Context、Run和MAF等模块，也写了模块责任，却没有从普通用户或新开发者的视角说明：前端页面拿着什么对象、请求跨过边界后变成什么对象、后端保存什么、Agent里面由哪些部件组成、一次交互结束后留下哪些事实。

### 错误本质

模块视图回答“责任放在哪里”，对象视图回答“系统处理的东西是什么”，运行视图回答“这些东西怎样随时间变化”。三者缺一不可。只有模块名和箭头时，熟悉架构术语的人可以自行脑补，但小白会把Message、Session、Thread、Run、Agent和Workflow混成同一个“聊天过程”；开发也容易在不同模块重复创建含义相近的对象。

### 可能后果

1. 用户无法判断架构是否真的覆盖自己的使用场景，只能被动相信术语。
2. 开发不知道DTO、领域对象、运行时对象和前端投影能否共用，产生跨层类型污染。
3. Product Session、AG-UI Thread、MAF AgentSession和Product Run再次被混用。
4. Agent被当成拥有全部历史、权限、工具、副作用和产品终态的万能黑盒。
5. 模块详细设计时才发现缺少对象、生命周期或映射关系，导致计划返工。

### 正确规则

1. 总体架构必须同时提供3层阅读路径：系统/模块关系、模块内部对象、一次真实请求的对象流和状态变化。
2. 每个核心对象至少用普通话回答6个问题：它是什么、谁创建、谁拥有、保存在哪里、活多久、用户能否看见。
3. 明确区分4类形态：前端展示模型、传输DTO或协议事件、产品领域对象、MAF/Worker运行时对象；名字相似也不能默认复用同一个类。
4. `Agent`必须展开为可配置身份与指令、模型客户端、上下文输入、运行Session、工具与执行桥、模型/工具循环、Workflow/Checkpoint、事件和结果；同时列出它明确不拥有的产品事实。
5. 至少用1个具体用户输入，逐步展示对象的创建、转换、引用、持久化、失败与恢复，不能只写“前端调用后端，Agent返回结果”。
6. 对尚未冻结字段的对象使用“架构级候选”标记；解释对象边界不等于提前批准Schema。

### 强制检查

- 第一次接触项目的人能否说清Message、Interaction、Run、Attempt、AG-UI Thread和MAF AgentSession分别是什么？
- 能否从一个前端输入草稿一路追踪到User Message、ContextPackage、Product Run、MAF执行、Evidence、Assistant Message和Delivery？
- 每次对象转换是否说明为何转换，而不是让前端对象或外部SDK Payload直接进入领域层？
- Agent内部每个部件是否能对应到明确责任，且没有越权拥有Approval、Evidence或产品终态？
- 文档是否提供面向小白的例子，同时仍保留架构师和开发需要的所有权、合同与生命周期信息？

## 13. 反例 010：给出对象词典，却没有把一次用户操作真正跑通

### 错误触发

为了让架构更容易理解，文档增加了前端对象、协议对象、产品对象和Agent部件清单，也画出对象流；但读者仍不知道：Agent为什么有Session和Tool相关能力，后端为什么需要数据库，用户点击发送或批准后实际调用哪些组件，请求如何到达模型，Provider响应由谁解析、怎样变成产品事实并最终显示在页面上。

### 错误本质

对象词典解决“名词是什么”，不能替代运行说明中的“谁在什么时候拿什么做了什么”。真正可理解的架构必须同时回答：

1. 用户触发了哪个具体界面动作。
2. 前端哪个组件或函数接手，生成什么请求。
3. 请求在哪个协议入口终止，如何进入产品应用模块。
4. 每次数据库读写解决什么恢复、权限或事实问题。
5. Agent内的Session/History、Tool声明和模型循环分别做什么，哪些治理仍在Agent外。
6. 最终Provider请求由谁编译、审核和发送。
7. Provider返回的SSE/JSON、文本、Tool Call或结构化内容经过哪些解析和校验。
8. 哪些内容只是运行投影，哪些经过提交门成为Message、Evidence、Work或Run终态。
9. AG-UI事件如何回到前端状态，再由哪个组件渲染给用户。

### 可能后果

1. 小白仍只能记住名词，无法理解系统为何这样拆。
2. 开发会在前端、MAF和产品模块之间猜测谁负责Session、Tool和历史。
3. 模型返回文本可能被直接显示并误当正式产品事实，绕过解析、校验和提交门。
4. 数据库被理解成“统一存所有东西”的黑盒，或反过来完全遗漏持久化边界。
5. 当前纵向切片与目标架构被混为一谈，读者误以为未实现模块已经存在。

### 正确规则

1. 新手导读必须先用一个最常见动作——例如点击“发送”——按时间顺序讲完“前端→后端→模型→后端→前端”，再分别展开名词。
2. 每一步至少标明：触发动作、负责组件、输入、处理、输出、读写的Store和用户可见变化。
3. Agent的Session能力必须拆成MAF `AgentSession`/History Provider/Workflow Checkpoint，并与Agent外的Product Session管理对照说明“为什么都需要”。
4. Agent的Tool能力必须拆成模型可见Tool定义/调用循环，与Agent外的Tool Catalog、Policy、Approval、Execution Ledger和对账，并说明“模型提出调用不等于产品授权执行”。
5. 后端存储必须同时说明逻辑Store和物理实现：Product Store、Runtime Event Store、MAF History/Checkpoint Store、Artifact Store各自保存什么；物理共用SQLite也不能合并所有权。
6. 响应回程必须至少拆成4步：Provider协议解码、MAF运行内容归一化、产品候选解析/校验与提交、AG-UI投影和React渲染。
7. 同一条说明必须分别标出“当前代码实际经过的组件”和“目标架构审核通过后要增加的组件”，不能用目标图冒充现状。

### 强制检查

- 用户点击“发送”后，能否逐步说出前端函数、HTTP端点、Workflow/Agent、Provider Transport和返回渲染组件？
- 用户点击“批准”后，能否解释为什么发送的是同一份已审核Body，以及哪里记录Attempt？
- 能否回答Agent为什么需要AgentSession/History，同时产品为什么还要Product Session数据库？
- 能否回答Agent为什么声明Tools，同时产品为什么还要独立Tool执行模块？
- 能否指出模型原始响应在哪一层完成协议解析、内容归一化、领域校验、持久化和用户显示？
- 当前没有Product Schema这一事实是否在链路图中清楚标出？

## 14. 反例 011：允许声明系统无法执行的Tool

### 错误触发

模型调用审批页允许用户自由新增Tool、手填`name`和参数Schema，但后端没有对应的真实执行器、Provider原生能力或权限绑定。

### 错误本质

Provider请求里的Tool定义不是普通提示词，而是系统向模型声明的可调用能力合同。模型看到定义后可能返回Tool Call；如果系统无法执行，审核页就展示并发送了虚假能力。允许编辑发给模型的内容，不等于允许凭空创造不存在的执行能力。

### 可能后果

1. 模型选择不存在的Tool，运行没有文本结果或错误结束。
2. Tool名称、Schema和真实执行器漂移，参数无法校验或被错误解释。
3. 用户误以为声明Tool等于已经实现并授权执行。
4. Provider JSON可以绕过可读视图，重新塞入没有绑定的Tool。

### 正确规则

1. Tool定义只能投影自服务端已注册的Tool Catalog或当前Provider明确支持的原生Tool。
2. 可读视图选择Tool身份，不允许自由填写Tool名称或生成`new_tool`占位能力。
3. 当前没有真实Tool目录时，`tools`必须为空；可读视图和服务端保存校验都要拒绝非空定义。
4. 模型提出Tool Call不等于获得执行授权；仍需参数、权限、风险、幂等和Approval检查。
5. Tool定义修改必须保持与真实执行合同兼容，并产生新版本、Hash和审批。

### 强制检查

- 每个发送给模型的Tool能否定位到真实执行器或Provider原生能力？
- 修改后的名称和Schema是否仍与执行合同绑定？
- 没有可用Tool时，可读视图和Provider JSON是否都无法声明虚假Tool？
- 模型返回Tool Call后，是否存在明确的审批、执行、结果和下一次模型调用路径？

## 15. 反例 012：把不同Provider都当成同一种OpenAI协议

### 错误触发

Provider和模型虽然都号称“OpenAI兼容”，系统却固定向所有Provider发送Responses API的`instructions + input`，或者在审批后才由Transport转换成Chat Completions的`messages`。

### 错误本质

“OpenAI兼容”不是单一请求合同。Responses API与Chat Completions具有不同的端点、消息字段、内容类型和参数能力。审批对象如果不带协议，轻则请求得到404/参数错误，重则可读视图和真正HTTP Body不一致。

### 正确规则

1. Provider目录必须显式声明协议；当前支持的每种协议都有独立编译、能力和校验合同。
2. 草稿在选择Provider时就转换成目标协议的规范请求，不能等审批后再暗中转换。
3. 可读视图、Provider JSON、Hash、发送字节和目标端点必须共同由`Provider + protocol + canonical body`决定。
4. 跨协议切换要保留Instructions、消息和来源语义，同时删除目标模型不支持的参数并重新审批。
5. 每种已声明协议至少有自动合同测试和1次真实Provider浏览器回合。

### 强制检查

- 当前Provider实际支持的是Responses、Chat Completions还是其他协议？
- 审批页展示的是目标协议的最终Body，还是某个中间形态？
- 切换协议后，system/Instructions、消息来源、参数能力和Endpoint是否一起变化？
- Transport是否逐字节发送已审批Body，而没有再次改写？

## 16. 反例 013：把Workflow选择、Prompt发送和运行展示混成一个动作

### 错误触发

把用户在聊天框点击发送直接描述为“系统自动触发某个Workflow”，或者把Workflow Canvas设计成离开聊天后再选择和启动Workflow的独立页面，却没有在Prompt发送前让用户明确看到并选择本次要使用的Workflow。

### 错误本质

混淆了3个不同环节：

1. `Workflow Definition`说明系统有哪些可运行流程。
2. `Workflow Selection`由用户在发送Prompt前选择本次采用哪个流程，并把Workflow ID和版本绑定到本次执行请求。
3. `Workflow Run / Workflow Run View`在发送后运行已选流程，并投影真实节点、暂停、审批、结果和失败。

未来Agent可以根据意图推荐Workflow，但“推荐”不是“选中”，更不是执行授权；在用户接受前不能静默改变本次运行路径。

### 可能后果

1. 用户不知道Prompt会进入哪个Agent、Tool、权限和审批流程。
2. 同一Prompt无法解释或复现为什么走了不同路径。
3. Workflow Run View既兼任选择器又成为独立运行器，割裂聊天入口。
4. Agent未来的推荐被误实现成自动执行，绕过用户控制。
5. Workflow切换发生在Run创建之后，导致版本、权限、Hash和Trace无法稳定绑定。

### 正确规则

1. Prompt发送区必须展示当前选择的Workflow；没有合法选择时不能创建Run。
2. 当前只有一个Workflow时仍显示其名称和版本，不因为选项唯一就把选择事实隐藏掉。
3. 点击发送时，把`workflow_id + workflow_version + prompt/context`共同固化为本次Run输入；Run开始后不能静默换Workflow。
4. 右侧Workflow Run View只展示当前或历史Run实际采用的Workflow，不承担另一套脱离聊天的启动入口。
5. 未来Agent推荐必须显示推荐原因，并由用户接受后才更新选择；推荐行为本身不创建Run。

### 强制检查

- 用户点击发送前，能否明确说出本次选择了哪个Workflow？
- Product Run能否追溯到被选择的Workflow ID和版本？
- Workflow Run View展示的是否正是本次Run绑定的Workflow，而不是页面默认或静态目录？
- Agent推荐Workflow时，用户未接受是否绝不会改变本次选择或启动执行？

## 17. 反例 014：把左右分栏里的右侧区域直接命名为Canvas

### 错误触发

用户用ChatGPT Canvas举例说明“左侧对话、右侧呈现”的布局关系后，方案持续把右侧Workflow运行区域称为Canvas，并进一步把Workflow节点图、审批编辑器和未来白板能力都归入同一个Canvas概念。

### 错误本质

混淆了3个层次：

1. `Workspace / Workbench`是页面布局容器，负责承载右侧不同功能视图，本身不拥有业务内容。
2. `Workflow Run View`是运行投影，展示某个Product Run对应的Workflow节点、状态、审批和可公开输入输出。
3. `Canvas`是未来可独立创建、保存、版本化和协作编辑的空间型Artifact，例如白板的节点、连线、分组、便签和嵌入内容。

### 可能后果

1. 前端用一个巨型Canvas状态同时管理布局、运行态和白板内容，形成新的事实源混乱。
2. Workflow结束后不知道右侧内容应该消失、保存为Trace，还是保存成白板。
3. Canvas无法建立独立ID、版本、权限、协作和来源关系。
4. Workflow运行数据被错误写入白板Artifact，或白板编辑被误当成Workflow控制事件。
5. 后续Artifact预览、Evidence、文档编辑和真正白板都被迫复用一个含义不清的组件。

### 正确规则

1. 整体页面称为Workspace；右侧可伸缩容器称为Workbench或右侧工作区。
2. 当前承载的是Workflow Run View，不使用Canvas命名。
3. Canvas保留为独立产品对象：拥有稳定ID、版本化内容、权限和持久化生命周期；视口缩放等临时UI状态与权威画布内容分开。
4. Workflow可以读取、创建或修改Canvas，但必须通过明确命令、工具和审核合同；Canvas不保存Workflow运行权威状态。
5. 同一个Workbench未来可以切换Workflow Run View、Canvas、Artifact Preview或Evidence View，容器和内容对象不能合并。

### 强制检查

- 讨论“右侧区域”时，指的是布局容器、Workflow运行视图，还是持久Canvas对象？
- Workflow结束后，哪些信息进入Trace，哪些成为Artifact，哪些只是临时前端状态？
- Canvas是否拥有独立于Session和Run的ID、版本、权限与生命周期？
- Workflow修改Canvas时，是否经过可追溯的命令或Tool执行边界？

## 18. 反例 015：用术语表代替可运行的概念空间

### 错误触发

项目列出一批名词和一句话解释，却没有说明概念不是什么、与相邻概念的关系、谁拥有事实、用户能执行什么动作，以及概念定义与当前实现是否一致；或者把全部概念、项目状态和源码事实堆进一个大文件。

### 错误本质

普通术语表只能解释词，不能建立协作语义边界。Chat同时存在产品对象、前端投影、网络DTO、MAF运行对象和外部平台对象；同名不代表同一职责。概念空间还必须可发现、可按需读取、有唯一正文和生命周期，并把概念状态与实现状态分开。

### 可能后果

1. 用户和AI使用同一个词却批准了不同对象或动作。
2. UI、API、数据库和MAF类型复用一个名字，形成权限、状态和恢复混乱。
3. 概念正文复制项目进度，更新后产生第二套事实源。
4. 所有概念被每轮全部加载，反而增加理解和上下文成本。
5. 新词未经确认直接进入架构和代码，后续只能破坏性改名。

### 正确规则

1. 根方法、发现索引、概念簇、稳定产品事实、实现状态和源码证据分开拥有。
2. 只为真实反复混淆、影响权限/状态/接口或需要独立生命周期的概念建资产。
3. 每个概念至少说明定义、反定义、关系、使用、正反例、状态、实现和验证。
4. AI按当前任务读取相关概念簇，不通读全部资产；面向用户仍就地解释。
5. 新概念先是候选，用户确认后才有效；源码存在不能代替概念审核。

### 强制检查

- 新用户能否解释概念是什么、不是什么、谁拥有、活多久和能执行什么动作？
- 索引是否只做发现，而没有复制正文？
- 概念正文是否把产品责任、项目进度或源码行为变成第二事实源？
- UI按钮是否使用“动作 + 对象 + 后果”，而不是含糊的“确认、继续、重来”？
- 发生用户纠正或源码冲突时，能否定位到唯一正文并将概念标为修订中？

## 19. 反例 016：把逐次模型调用审批做成所有Workflow的隐藏全局门

### 错误触发

用户已经选择一个多Agent Workflow，点击发送后却连续看到多个外观完全相同的“确认真正发给模型的内容”弹窗；界面没有事先说明该Workflow会调用几次模型，也没有区分“确认运行这个Workflow”和“审核某一次Provider请求”。

### 错误本质

混淆了3种不同决定：

1. `Workflow Selection`决定本次采用哪个Definition和版本。
2. `Workflow Run Authorization`允许某一版本Workflow在明确的Agent、模型、Tool、调用次数和成本边界内运行。
3. `Model Call Approval`逐字绑定某一次已经生成的Provider请求、版本和Hash。

多Agent Workflow可以包含多次模型调用，因此“一次Workflow运行”不天然等于“一次模型调用审批”。反过来，用户一次批准整轮运行，也不能被描述为已经逐字审核了尚未生成的下游Prompt；下游Prompt可能依赖上游模型结果，启动时并不存在。

### 可能后果

1. 用户误以为系统嵌套运行了另一个“发送前可编辑Prompt”Workflow。
2. 低风险固定流程被重复弹窗打断，Workflow自动编排价值消失。
3. 产品声称“一次确认”，实际却无法说明用户批准的是精确请求还是受约束的运行范围。
4. Workflow增加Agent、Tool或调用次数后，授权范围被静默扩大。
5. 配置页只显示全局“每次调用”，用户在选择Workflow时无法预期交互成本。

### 正确规则

1. 模型审批策略必须成为Workflow Definition或其治理策略中的显式、版本化属性，不得只藏在Agent Executor实现里。
2. 至少区分`each_model_call`和`bounded_run_once`：前者逐次展示、编辑并绑定精确Provider请求；后者只对Workflow版本、输入、Agent/Profile Revision、Provider/模型、Tool范围、最大调用次数、成本和确定性规则做一次有界授权。
3. `bounded_run_once`模式必须明确说明：后续实际Provider请求在生成后可查看和审计，但用户没有在发送前逐字编辑每一份请求；任何界面都不得把它宣传成精确Payload审批。
4. 任一已绑定要素变化、运行尝试扩大Tool/模型/调用次数或越过成本上限时，整轮授权立即失效并重新暂停。
5. Workflow选择器在发送前展示审批模式和预计模型调用次数；运行视图把Run授权与模型调用事件使用不同名称和视觉层级。
6. 选择Workflow不是授权运行；一次Run授权也不是未来所有Run或所有模型调用的永久同意。

### 强制检查

- 用户点击发送前，能否说清本次是“逐次请求审批”还是“整轮有界授权”，预计会被打断几次？
- 如果只确认一次，授权对象是否绑定Workflow版本、输入、Agent/模型/Tool、最大调用次数和成本？
- 依赖上游模型输出的下游Prompt是否被诚实标为“运行中生成”，而不是伪装成启动时已审核？
- Workflow内部新增一次模型调用或Tool后，旧授权是否必然失效？
- 审批弹窗和运行视图是否能让用户区分Workflow运行授权与某次Provider请求审批？

## 20. 反例 017：只完成回复，却没有完成回合沉淀和工作连续性

### 错误触发

主Workflow只描述“接收输入、识别意图、获取上下文、调用Agent、回复用户、更新任务”，但没有定义原始会话怎样保留、本轮主题怎样提取、摘要何时生效、哪些信息进入哪类Memory、怎样识别并恢复一个开发到一半的项目，以及多个状态写入失败时怎样避免半提交。

### 错误本质

把4种不同事实混成“长期记忆”或全部省略：

1. Conversation保存本轮原始Message和发生过什么。
2. Interaction Summary是从本轮派生的主题、决定、开放问题和关联候选，服务检索与压缩，但不能删除原始事实。
3. Accepted Memory保存获准跨会话复用的稳定信息，必须有来源、范围、版本和失效规则。
4. Project/Work状态保存项目目标、进度、当前任务、阻塞、计划和证据，由领域命令和提交门维护，不能靠摘要文本代替。

“简单询问不创建任务”只表示不应误改Project/Work；它仍然是一次正式Interaction，必须保留原始消息，并可产生Session级主题摘要。反过来，模型生成了摘要也不表示其中的偏好、结论和完成状态自动成为Accepted Memory或Work事实。

### 可能后果

1. 下一轮只能重读全部聊天，Token不断增长，或因为完全没有摘要而失去连续性。
2. “继续贪吃蛇”无法定位项目，只能依赖当前Session标题或模型猜测。
3. 摘要错误会静默污染用户偏好、项目进度和任务完成状态。
4. Assistant消息已经显示成功，但摘要、项目状态或索引写入失败，产生不可解释的半提交。
5. 原始消息被摘要替代后，用户无法追溯、纠正或重新生成派生信息。

### 正确规则

1. 每个已接纳Interaction都保存原始User Message；成功、失败、取消和放弃分别保存可见结果及状态，摘要不能替代原始记录。
2. 每次Interaction进入终态后运行回合沉淀：提取主题、事实候选、决定、开放问题、项目/Work关联候选、状态变更候选和下一步；无价值字段允许为空，不能为了“有摘要”编造内容。
3. 下一轮采用两阶段上下文：先读取当前输入、最近Interaction摘要、项目/Work目录和轻量检索索引识别候选目标；确认目标后再装配该项目的Work、计划、证据、Accepted Memory和必要原始片段。
4. 摘要、Memory和Work分别版本化并保存来源。摘要可以由确定性规则自动提交为派生索引，但Memory接受和高影响Work状态变化必须经过规则或HITL提交门。
5. 产品事务只原子提交本地权威事实和Outbox，不把模型、向量库或外部Tool调用放进数据库事务；派生索引由Outbox异步更新，失败可重建，不能回滚已经接纳的原始消息。
6. 回合对用户“完成”前必须明确完成门：Assistant Message、Run终态和必要的Work变更已经原子提交；摘要/索引若允许异步，必须显示待处理或失败并可重建，不能静默丢失。

### 强制检查

- 一个简单问答结束后，原始Message、Interaction Summary、Accepted Memory和Work分别发生了什么？
- “继续某项目”时，系统是否先召回候选，再在目标明确后加载项目完整上下文，而不是先加载所有项目？
- 摘要中的项目进度或用户偏好是否仍只是候选，除非通过对应提交门？
- 摘要生成失败、索引写入失败、Work CAS冲突时，是否保留原始会话并能重试派生处理？
- 用户能否查看摘要来源、纠正错误关联，并让后续Context停止采用错误信息？

## 21. 反例 018：把HITL做成固定弹窗，把Prompt组装做成历史拼接

### 错误触发

系统在意图、上下文、计划、模型调用、Tool和状态提交等每个位置都能暂停，却把“存在HITL点”实现成“每轮必定弹窗”；同时仍沿用传统Agent做法，把System Prompt、当前User Prompt和完整Session History不断追加后直接发送给执行Agent。

### 错误本质

混淆了3层责任：

1. HITL决策点表示系统在这里可能需要人的判断，不表示所有用户、项目和风险等级都必须暂停。
2. User Prompt是用户自然表达的输入源，不是已经具备背景、范围、计划、权限和验收标准的执行说明。
3. Conversation History是完整证据源，不是每次模型调用都应继承的默认工作记忆。

真正的产品能力是把外部保存的Intent、Project/Work、TaskPlan、Decision、Memory、Evidence和文件索引按当前目标选择，形成最小充分ContextPackage，再编译成可审核、可复现的ExecutionDraft/RunSpec。模型上下文是一次有预算的工作集，不是产品状态库。

### 可能后果

1. 用户被低价值弹窗淹没，最终形成机械批准，HITL失去风险控制意义。
2. Session越长Token越大，旧主题、错误尝试和无关闲聊持续污染执行。
3. 不会写Prompt的用户把需求表达不完整，Agent只能猜背景、范围和完成标准。
4. Pi等执行Agent拿到大量对话却不知道当前任务、应该先读哪些文件、允许做什么以及怎样证明完成。
5. 用户跳过一次审核后系统无法说明依据、作用域和何时重新暂停，形成隐性永久授权。

### 正确规则

1. 每个HITL点声明决策类型、风险、可见候选、允许动作和策略；策略至少支持始终暂停、风险/不确定性触发、自动通过和继承上级配置。
2. 配置具有作用域和优先级：系统不可跳过底线 > 本次Run > Project/场景 > Workflow > 用户默认；自动通过必须记录命中的策略、输入版本和决定结果。
3. 权限扩大、破坏性或外部副作用、跨项目数据、版本冲突、预算越界和结果未知不能被普通“跳过”永久豁免。
4. 建立Prompt/RunSpec编译链：User Prompt + Intent + Project/Work状态 + TaskPlan + ContextPackage + Capability/Policy + Validation Contract -> ExecutionDraft -> 用户修正/策略批准 -> 不可变RunSpec -> Provider专用ModelCallDraft。
5. ContextPackage优先提供背景、目标、已接受决定、当前状态、范围、下一步骤、文件/知识引用、验证标准和停止条件；大文件和低相关历史通过Tool按需读取，不提前复制进Prompt。
6. 场景范式决定怎样拆任务和组织RunSpec，例如问答、研究、计划、软件开发、学习、审校和持续项目推进；范式可以复用Workflow/Agent/Executor，但不能绕过产品状态和审批策略。

### 强制检查

- 某个HITL点为什么暂停，用户能否只对当前Run、当前Project或同类低风险动作选择自动通过？
- 自动通过后，Trace能否说明命中了哪条策略、批准了哪个版本以及什么变化会重新暂停？
- 发送给Pi Agent的是不是一份说明背景、目标、范围、步骤、资源、验证和停止条件的工作包？
- 删除完整Session History后，系统是否仍能从结构化Project/Work/Memory/Evidence重建本轮最小充分上下文？
- 大文件和旧历史是否以可定位引用交给Agent按需读取，而不是为了“完整”全部塞入模型上下文？

## 22. 反例 019：把澄清问题塞进审批卡，却不给用户回答入口

### 错误触发

用户明确询问“我有哪些项目”，意图Agent却生成“你是想查看现有项目，还是开始新项目或任务”；系统随后把这个澄清问题放进意图审批卡，只提供“停止Run、修改内容、接受并继续”，没有文字输入或选项回答入口。

### 错误本质

混淆了3种不同交互合同：

1. `Intent Candidate审核`让用户接受或修正系统的理解，处理对象是一个已有候选版本。
2. `Clarification Request`向用户索取系统尚不知道的新信息，处理结果必须是一条新输入，而不是对旧候选点击“接受”。
3. `明确产品查询`已经给出目标，例如查看项目列表；系统应读取权威产品事实，不能因为当前没有可匹配Project就改问用户是否想新建。

### 可能后果

1. 用户看到了一个问句，却没有任何能够回答它的控件，只能错误地把“接受”当成答案。
2. 点击接受后，Workflow仍拿不到“查看现有列表”这一新信息，继续沿错误Intent运行。
3. 简单查询被误建成任务、误关联Project，或为了填补数据缺口让模型编造项目。
4. 真正需要澄清时，用户的简短回答无法关联上一个开放问题，下一轮再次重复询问。
5. HITL记录把“提供信息”和“授权后果”混在一起，Trace无法解释用户到底决定了什么。

### 正确规则

1. 审批请求与澄清请求必须使用不同交互合同、动作和文案；`accept/revise/skip/cancel`不能代替文字回答。
2. 真正需要澄清时，要么在请求中提供文字/明确选项并用“回答并继续”，要么结束当前Interaction，把问题提交成Assistant Message，让用户在聊天输入框开始下一轮回答。
3. 澄清回答必须作为新的User Message或等价的版本化输入进入Intent重判；不能仅把旧Intent标为accepted。
4. 未解决澄清要以可追溯的开放问题进入下一轮最小Context，确保“第一个”“就看列表”这类短回答仍能关联原问题。
5. 对“我有哪些项目/查看项目列表”这类明确产品命令设置确定性意图护栏；模型输出只能补充解释，不能把查询改成新建分支。
6. 当前没有正式Project事实时，系统必须明确返回“没有已创建的正式Project”，并把对话摘要中的名称标成候选，不能冒充正式项目。

### 强制检查

- 页面出现问号时，用户是否一定有与问题类型匹配的回答入口？
- 点击“接受”究竟接受了哪个候选和后果，是否被错误用于提供新信息？
- 明确的列表、查看、状态查询是否可能被模型改判成创建或执行？
- 用户下一轮只回答“第一个”时，系统能否稳定带回上一个未解决澄清？
- 权威Project目录为空时，回答是否诚实区分正式Project与对话中识别的候选？

## 23. 反例 020：让Agent从聊天摘要猜Project、Task、笔记和长期Memory

### 错误触发

用户询问“我有哪些项目”“这个任务做到哪了”或“我最近在学什么”时，系统只把最近消息、回合摘要或MAF Session交给模型，让模型自行推断；需要更新进度或笔记时，也让Agent直接改Prompt内的一段文本或文件。

### 错误本质

把4种不同责任混成了“模型记忆”：

1. Product Session和原始Message是对话证据，不是Project目录。
2. Turn Summary是派生索引，不是已经接受的Work事实。
3. MAF AgentSession/Workflow Checkpoint只负责运行时连续性，不拥有产品级Project、Task、Note或Memory生命周期。
4. Agent Harness应是访问产品能力的受治理合同，不是另一个无版本文本仓库。

### 可能后果

1. Agent会遗漏项目、编造进度，或把一次闲聊误当成正式任务。
2. 同一用户跨Session、跨天或换Worker后得到不同答案。
3. 项目状态、学习笔记和长期Memory没有来源、版本、冲突检测或撤销路径。
4. Agent的写入越过HITL和产品事务，失败后出现聊天说已完成但权威状态没有更新的假成功。
5. Prompt越来越长，却依然无法可靠回答资源列表、当前状态和下一步。

### 正确规则

1. Chat必须拥有一套产品级Harness：用明确的Project、Work、Plan、Note、Memory及其关联对象承载用户的想法、任务、项目和学习资料。
2. Harness以版本化应用命令和查询工具暴露给Agent；只读查询返回权威事实，写操作生成候选或命令并经过策略、HITL、CAS和事务提交。
3. Agent先查询轻量资源目录，再按已识别或用户确认的目标加载详细状态；不能默认把全部Project、Note和历史会话塞进模型上下文。
4. Project、Work、Note和Memory分别拥有状态机、来源、作者、版本、时间和Trace；摘要只能提出关联或变更候选。
5. 产品事务原子提交领域事实和Outbox，搜索索引、向量索引和外部同步异步派生且可重建。
6. MAF负责Agent、Workflow、Checkpoint和工具调用语义；Product Harness负责业务事实与治理，两者通过受测Tool合同连接，不能共享事实所有权。

### 强制检查

- “我有哪些项目”是否直接查询Product Harness，而不是让模型根据最近消息猜测？
- 跨Session、跨进程、跨几周后，Project/Work/Note/Memory是否仍有相同的权威ID、版本和状态？
- Agent提出的进度更新是否在用户可见、可修改、可拒绝后才成为正式事实？
- 摘要、索引或模型调用失败时，已经提交的原始Message和产品事实是否仍完整、可恢复？
- Harness工具是否区分只读查询与产生副作用的命令，并对写入执行授权、CAS和幂等检查？

## 24. 反例 021：虚拟环境异常后回退到系统Python

### 错误触发

项目虚拟环境中的原生扩展无法加载，开发者为了继续运行测试，改用`/usr/bin/python`、Homebrew全局Python或其他未锁定解释器，并把结果当成项目验证证据。

### 错误本质

解释器、架构、依赖锁和运行配置共同构成可重复工程环境。系统Python能启动，不表示它与`pyproject.toml`、`uv.lock`、MAF安装版和生产入口一致；宿主安全服务故障也不等于项目代码失败。

### 可能后果

1. 测试在错误依赖版本上通过，真实项目环境仍然失败。
2. Alembic自动生成和运行时使用不同SQLAlchemy/驱动版本，产生错误迁移。
3. MAF安装版行为被全局包污染，源码证据和实测证据失去对应关系。
4. 环境故障被误报成代码故障，或静态检查被冒充成完整测试。

### 正确规则

1. Python固定为`3.12.x`，由`uv`安装和选择；依赖严格来自当前`uv.lock`。
2. 所有Python命令通过项目虚拟环境解释器或显式`UV_PROJECT_ENVIRONMENT`执行，不隐式使用系统Python。
3. 环境损坏时创建新的项目专用虚拟环境并重新同步锁文件，不污染源码和私有配置。
4. macOS签名、Rosetta、网络或文件系统问题单独诊断；未运行的测试必须标为未运行，不能用`py_compile`替代。

### 强制检查

- `sys.executable`是否指向当前项目专用虚拟环境？
- Python是否为锁定的3.12架构，依赖是否由`uv.lock`解析？
- 测试、Alembic、服务启动和真实模型验证是否使用同一个环境？
- 宿主故障是否与代码失败分开记录，且没有静默降低验证标准？

## 25. 反例 022：功能不断增加，却没有同步收敛工程质量

### 错误触发

纵向场景和端到端测试已经增加，但路由、应用服务、Workflow定义和前端组件持续膨胀；团队仍然只用“功能跑通、测试通过”判断产品能否继续扩展。

### 错误本质

产品能力、工程可维护性和生产可运营性是3种不同的完成条件。场景跑通只能证明一条行为链在当前快照成立，不能证明模块边界、质量门、故障定位和长期演进已经可靠。

### 可能后果

1. 新功能被迫依赖越来越大的文件和隐含调用顺序，修改一个场景容易影响另一个场景。
2. 进程恢复、Tool副作用、Evidence和多入口建立在不可观测、不可度量的基础上时，故障只能靠猜。
3. 缺少自动质量门会让“本机通过”无法转换成“每次变更都能重复证明”。
4. 文档不断追加完成记录却不清理过期事实，设计者和开发者会依据错误状态继续工作。

### 正确规则

1. 每个重大纵向里程碑完成后，审计模块职责、依赖方向、文件规模、错误边界、事务边界和可替换接口，并把整改登记为正式Todo。
2. 审计必须同时覆盖代码质量、架构、注释与API说明、日志与指标、调试手段、测试覆盖、CI和文档一致性，不能只统计测试数量。
3. 重要结论必须有源码、配置、测试或运行结果作为证据；参考项目只为其真实覆盖的范围背书。
4. 大规模重构必须先给出用户场景、目标、不变量、迁移方案和回归验证，不能借“优化代码”改变已批准的产品语义。
5. 工程收敛完成前，新高风险能力应优先落在已经明确的应用边界中；边界尚未建立时，先完成最小的无行为重构和质量门。

### 强制检查

- 当前最大的模块、组件和样式文件分别承担了多少职责，是否存在可独立测试的拆分边界？
- 每次提交是否自动执行格式、Lint、静态类型、单元/合同/集成测试、覆盖率和生产构建？
- 一次用户操作能否通过稳定的关联ID贯穿Product Session、Agent Run、Workflow Run、Execution Request和Worker Job？
- 错误是否使用稳定代码和脱敏消息，还是由路由直接把内部异常字符串返回给前端？
- 文档中的“当前状态”和源码、迁移、测试是否一致，已完成能力是否仍被写成未来Todo？

## 26. 反例 023：用“大文件先跑通、以后再整理”持续制造不可维护代码

### 错误触发

为了快速增加场景，把新的查询、状态转换、数据库事务、外部调用、日志和UI状态继续写进已有大型Application Service、React Hook或页面组件；等文件失控后，再按行数机械拆成许多没有清晰所有权的`utils`、`helpers`和薄转发层。注释只复述代码，日志只打印异常字符串。

### 错误本质

代码规模只是风险信号，不是模块边界。可维护拆分应围绕“谁拥有状态、谁开启事务、哪个变化原因会让代码一起修改、失败后由谁恢复”建立边界。机械拆文件会增加跳转和循环依赖，却不减少职责；完全不拆则让每次修改都扩大回归面。注释与日志也必须服务于不变量、决策和故障定位，不能成为噪音。

### 可能后果

1. Governance、Harness、Workflow和前端Agent状态相互调用，无法单独测试或替换。
2. 一个事务被多个协调器分别提交，产生半完成产品事实或重复Outbox。
3. Hook同时拥有网络订阅、恢复、审批和页面交互，断线修复会破坏普通发送。
4. 开发只能靠全文搜索和猜测理解链路，新功能持续复制旧逻辑。
5. 生产故障只有“失败了”的日志，没有Run、Attempt、Job、节点和状态转换关联。
6. 规范过度反应，产生接口套接口、每个方法一个Service和大量无价值样板。

### 正确规则

1. 模块按业务能力、状态所有权、事务边界和变化原因拆分；行数只触发审查，不自动触发拆分。
2. Application Coordinator负责用例顺序和唯一事务所有权；纯规则、查询投影、Provider/Runtime适配和UI展示分别独立，不能反向依赖HTTP或页面层。
3. 拆分前先写不变量和行为指纹；拆分后用架构测试、合同测试和端到端场景证明状态机、Hash、事件、节点ID和用户行为没有漂移。
4. 只有确有替换边界、独立测试价值或第二实现时才增加接口；禁止Repository-per-table、Service-per-method和万能`utils`目录。
5. 注释解释“为什么、约束、恢复语义和非显然兼容原因”，不翻译语法；公开合同、事务所有者和高风险状态转换必须有Docstring或邻近说明。
6. 结构化日志只放在请求/命令入口、关键状态转换、外部调用、重试/接管/对账和失败边界；带稳定关联ID与结果，不记录密钥、完整Prompt、Provider Payload或隐藏推理。
7. 前端按Feature和运行责任拆分；生产代码在页面、Workbench和重型编辑器等真实加载边界延迟加载，不为了减小单文件制造微小Chunk。
8. 规范保持比例原则：规则必须对应已发生风险或明确质量保证，并能自动验证；不能为尚不存在的规模问题预建复杂平台。

### 强制检查

- 新增代码是否进入了正确的能力模块，还是因为“调用方便”继续堆进聚合文件？
- 当前用例只有一个事务提交者吗？被提取组件是否在不拥有事务的情况下擅自`commit`？
- 大文件审查后能否说清每个可拆边界的状态所有者和失败语义？说不清时不得机械切割。
- 新接口、抽象类或目录是否至少解决一个实际替换、测试或依赖问题？
- 注释能否帮助后来者理解不变量和取舍，日志能否用关联ID还原一次失败状态转换？
- 拆分后，架构依赖测试、合同指纹、自动E2E和生产构建是否共同通过？

## 27. 反例 024：把Kimi Print模式包装成“已审批的编码工具”

### 错误触发

为了快速让外部编码Agent工作，直接运行`kimi -p`或`--prompt`并解析
`stream-json`；看到结构化Tool Call后，就宣称模型请求和文件操作都已经进入Chat审批。

### 错误本质

Kimi `stream-json`是执行转录，不是权限协议。匹配安装版源码会在Print模式给Session设置自动权限，并为审批与提问安装Headless处理器；输出里出现Tool Call只说明它发生过，不能证明执行前经过了用户或Chat治理。ACP能够转发Tool权限请求，但本身也不公开Kimi内部完整Provider请求。

### 可能后果

1. Kimi在无人确认时修改文件或执行命令，而页面仍显示“受控工具”。
2. 把执行后的事件误当执行前授权，审批Hash与真实副作用没有绑定。
3. 用户以为每次模型Payload可见，实际只能看到Kimi对外公开的Agent/Tool事件。
4. CLI升级后权限语义变化，新Session继续沿用错误命令且没有版本证据。

### 正确规则

1. Codex默认通过工具Allowlist固定为`Read/ReadMediaFile/Glob/Grep`的Kimi Agent做非交互审查，并独立核对结论。
2. Kimi亲自修改时使用交互式TTY，逐项处理权限；不得用`-p`、`--auto`或`--yolo`做无人值守修改。
3. Chat产品接入使用ACP表达Session、Tool事件与Permission Request；需要逐次Provider Payload审核时另建可证明的Provider网关，不能从ACP能力类推。
4. 每次重要使用记录版本、调用模式、Session/退出状态和独立验证；升级后重新核对匹配Tag源码。

### 强制检查

- 本次Kimi是否只有任务需要的最小Tool Allowlist？
- `stream-json`是否被误写成了审批或安全协议？
- Kimi修改代码时是否存在真实的执行前权限处理和修改后Diff/测试？
- 对用户承诺的Provider可见性是否有实际网关字节证据？

## 28. 反例 025：已撤回的首条消息继续冒充Product Session标题

### 错误触发

新Product Session第一次接纳用户输入时，系统立即把该输入截断为会话标题；用户随后在发送前审批中放弃，原消息被标记为`withdrawn`并从可见历史隐藏，但标题仍保留这段已撤回内容。之后用户发送新的真实问题，侧栏继续显示旧标题。

### 错误本质

Product Session标题是可见导航元数据，不是某条Message的不可逆副作用。系统只实现了“首次接纳时生成标题”，却没有定义来源Message失效后的回滚、重算或来源标识；同时侧栏只显示标题和版本，没有展示可用于消歧的Product Session ID。

### 可能后果

1. 用户以为当前打开了另一个Session，或误判新问题被错误理解。
2. 多个相似标题无法稳定指向同一Product Session，用户和开发者只能靠猜。
3. 已撤回、敏感或仅用于验证的输入继续出现在导航区，违反用户对“放弃”的直觉。
4. 排查问题时把标题、AG-UI Thread和Product Session身份混为一谈。

### 正确规则

1. 自动标题必须记录来源Message ID或明确的生成版本；来源消息撤回时，事务内恢复默认标题或从下一条可见User Message重算。
2. 手工编辑过的标题不得被自动回滚；必须区分`auto`与`manual`标题来源。
3. 会话侧栏至少提供稳定短ID和复制完整Product Session ID的入口；短ID只用于人类沟通，后端仍使用完整UUID和可信Scope。
4. UI、日志和支持沟通使用限定名“Product Session ID”；不能用标题或当前同值的AG-UI `threadId`代替产品身份。
5. 在ID尚未向用户展示时，支持人员或Agent必须先用用户看得见的标题、消息片段和时间定位，再由系统查询内部ID；禁止反过来要求用户提供隐藏字段。

### 强制检查

- 撤回首条User Message后，会话标题是否仍泄漏或引用该消息？
- 第二条可见User Message提交后，自动标题是否能反映当前可见会话？
- 用户能否在不打开开发者工具的情况下复制完整Product Session ID？
- 两个同名Session能否通过可见短ID稳定区分？
- Agent要求用户指出某个Session时，所引用的定位信息是否真的在当前界面可见？

## 29. 反例 026：只记录“模型调用成功”，却无法解释结果从哪里来、后来去了哪里

### 错误触发

Provider请求返回成功后，系统只记录一次`completed`或把最终Assistant Message保存下来；没有保存发送、首字节、接收、解码的有序里程碑，也没有记录Provider可见输出是被采用、覆盖、拒绝还是仅用于摘要。排查时只能依赖已经滚走的终端输出，或从最终答案倒推模型做了什么。

### 错误本质

“Provider成功”“模型可见输出”“Workflow采用结果”和“Product提交结果”是4个不同事实。ModelCall Attempt拥有传输事实，Workflow节点拥有采用语义，Product Trace拥有产品状态转换，进程日志只用于运行诊断；任何一层都不能冒充另外三层。

### 可能后果

1. 无法判断最终回答来自模型、确定性查询还是模型输出被产品护栏覆盖后的结果。
2. Provider已经计费但解码或产品提交失败时，系统可能错误重试或产生双发送。
3. 模型返回了错误内容但后来被丢弃时，最终消息看似正确，设计者却无法发现模型或Prompt问题。
4. 终端重启、日志轮转或跨进程恢复后，历史故障无法重建。
5. 高频前端轮询淹没真正的Provider和状态转换日志，增加定位成本。

### 正确规则

1. 每次真实Provider调用持久化独立ModelCall Attempt，并按序记录`provider.dispatch`、`provider.receive`、`provider.decode`的开始和完成。
2. Attempt保存HTTP状态、Provider请求/响应ID、首字节与完成时间、用量、脱敏响应元数据、可见输出文本及SHA-256；不保存授权头、完整原始响应包或隐藏推理。
3. Workflow必须记录输出Disposition及原因，例如`accepted_as_intent`、`accepted_as_response`、`accepted_as_summary`、`rejected_invalid_output`或`overridden_by_deterministic_guard`。
4. 明确的权威产品查询应直接走确定性分支，并在治理视图显示模型调用数为0；不能先调用模型再用确定性结果掩盖无意义调用。
5. Product Trace是节点与产品状态的权威事实，持久JSONL日志是进程和外部传输诊断；两者用Session、Product Run、Run Attempt、Workflow、Executor和Model Attempt关联。
6. 活动Run的只读轮询保留Metrics和DEBUG可见性，不重复写入INFO；INFO保留命令、状态转换、外部调用、恢复和失败边界。

### 强制检查

- 任意最终Assistant Message能否回答“调用了几次模型、每次可见输出是什么、分别被如何处理”？
- Provider发出后进程丢失时，是否能区分未发送、已发送未确认和已解码未提交？
- 确定性Product查询是否明确显示0次模型调用？
- 日志与Trace是否都带足够关联ID，且不会记录密钥、完整Provider Payload或隐藏推理？
- 旧数据缺少某项证据时，系统是否诚实标记“历史未记录”，而不是根据当前代码反推历史事实？

## 30. 反例 027：用顺序列表或解释卡冒充Workflow结构图

### 错误触发

运行视图已经列出全部节点、实际路径和分支原因，但三块内容彼此分离：节点仍是纵向台账，实际路径仍是横向标签，选择依据仍是独立长卡片。用户必须在脑中重新拼出“从哪里分叉、走了哪条边、在哪里汇合”。

### 错误本质

Workflow是图，不是列表。状态、条件和原因只有放回节点与边的拓扑关系中才可被直观看懂；单独的解释文字即使正确，也不能替代结构表达。完整节点台账适合审计细节，不适合成为运行视图的第一信息层。

### 正确规则

1. 设计者运行视图优先用拓扑图表达真实Workflow Definition，并用Product Trace覆盖本轮状态；节点台账降为可展开的第二层。
2. 选择节点必须在图中同时显示候选边、声明顺序、公开条件、实际值、选中目标和未走原因；选中路径高亮，未选分支保留位置并弱化。
3. 为可读性增加的阶段容器只能标为布局分组，不能伪装成MAF节点；真实节点仍保留稳定`executor_id`。
4. 点击图上任一真实节点必须在同一视图打开公开输入、公开输出、运行事实和治理事实。
5. 图可以缩放和滚动，但正常比例的正文不得低于项目可读字号下限；不能通过默认缩小字体实现“全图塞进一屏”。

### 强制检查

- 不阅读节点台账，是否能指出起点、选择节点、4条候选边、本轮路径和后续结果？
- 选择原因是否附着在对应边上，而不是要求用户去另一张卡片对照？
- 未执行分支是否仍可见且明确标为“本轮未走”？
- 布局分组是否明确不是额外MAF节点？
- 点击图上节点是否能查看与该`executor_id`一致的Trace和治理内容？

## 31. 反例 028：把协作方法塞进大Prompt，再把全部上下文交给执行Agent

### 错误触发

系统已经拥有Project、Work、Note和Memory，却继续把“软件项目怎样推进、学习怎样复习、文档必须怎样自检”等方法写成散落的System Prompt、Agent Instructions或Memory文本；准备执行时，又把整份RunSpec、全部Session History、所有Project详情和完整用户偏好交给每个Agent或pi。

### 错误本质

混淆了4种责任：

1. Product Harness保存用户当前有哪些项目、任务、笔记和已接受事实。
2. 协作协议说明某类工作应该怎样理解、规划、执行、验证和回写。
3. RunSpec固定一次运行的目标、范围、能力、预算、验证和提交合同。
4. 步骤输入只投影当前Agent或Runtime完成本职责所需的信息与权限。

Memory可以记录“用户偏好简洁回答”，但“所有设计文档必须有metadata并自检”是需要版本、作用域、验证器和失败动作的可执行规则。RunSpec是整轮合同，也不表示Planner、Executor和Reviewer应该共享同一份上下文和写权限。

### 可能后果

1. 同一规则在Prompt、代码和Memory中出现多个版本，无法判断执行时采用哪一份。
2. Project、学习和周期任务只能依赖模型“记得方法”，跨Session或更换Agent后行为漂移。
3. Agent看到无关Project、私密Note和过多历史，产生越权、泄漏和错误关联。
4. Planner、Reviewer继承执行Tool权限，模型判断越过产品授权边界。
5. 上下文随时间线性增长，目标、验证和停止条件反而被噪声淹没。
6. 用户修改一条标准后，旧Run和Approval无法准确失效或重放。

### 正确规则

1. 协作方法必须成为版本化、可查看、可绑定的协议候选；用户批准前不能提前创建正式Schema，但不能继续把它只藏在Prompt字符串中。
2. 协议只引用现有Project、Work、Plan、Note、Memory、Evidence等产品对象，不为每个场景复制第二套生命周期。
3. 可执行用户标准进入带作用域和验证方式的协议规则；描述性偏好和稳定事实仍走Memory候选与接受门。
4. Context先轻量召回候选，再加载已绑定目标的详细事实；完整History保留为证据，默认不进入模型。
5. 从已批准RunSpec为Intent Agent、Planner、pi、Reviewer和Summary步骤分别编译最小输入、能力、预算、输出和停止条件。
6. 确定性代码拥有状态机、权限、Hash、事务和验证门；模型输出仍是候选，Tool和长期状态写入必须经过各自授权。
7. 协议、Context、Plan、能力或验证规则变化时，受影响的Draft、RunSpec和Approval必须失效。

### 强制检查

- 项目管理、学习管理和周期工作的方法能否定位到明确revision和作用域，而不是全文搜索Prompt？
- 用户说“以后文档必须自检”后，规则能否进入ExecutionDraft、执行层输入、Validator和失败修复，而不只是Memory？
- Intent Agent、Planner、pi和Reviewer分别收到了哪些Context和Tool，是否遵循最小权限？
- 删除完整Session History后，能否从Harness、协议和来源引用重建当前最小工作包？
- 同一规则或Context变化后，旧RunSpec与Approval是否必然失效？

## 32. 反例 029：把Chat Harness理解成Prompt模板、MAF封装或一组信息卡

### 错误触发

用户提出“Chat由Chat Harness与MAF驱动的AI系统共同组成”后，实现直接新增一个
`HarnessPrompt`、把规则拼进System Instructions，或只在前端做Project、Memory和Context卡片；
MAF Workflow、执行Worker、产品事实和提交门仍各自维护一套隐含语义。

### 错误本质

Chat Harness是Chat的产品语义与协作控制系统，不是一个字符串、SDK概念或页面。它必须同时回答：

1. 用户已经拥有哪些Project、Work、Plan、Note、Memory、规则、Evidence和Delivery事实。
2. 当前意图应采用哪一版协作协议、哪些Context和哪些HITL下限。
3. 怎样把同一事实和协议分别投影给用户、Chat Workflow与执行层。
4. 模型或Runtime返回后，哪些只是候选，哪些经过验证和授权后可以提交。

MAF负责Agent、Workflow、Session、Tool调用语义与Checkpoint；它不替Chat拥有上述产品事实。
前端是Harness与Runtime的交互投影，也不成为第三个事实源。执行层属于完整Chat系统，但处在独立
权限、恢复和副作用边界内。

### 可能后果

1. 用户在界面上选择的规则与真正发给Agent的内容不一致。
2. 更换Workflow、模型或执行Runtime后，项目方法和用户标准丢失。
3. Agent在自然语言中宣称“已记录、已完成”，数据库却没有对应事实或Evidence。
4. 前端勾选产生第二套Context状态，刷新、跨Session或跨进程后无法恢复。
5. 一个大型Harness Service继续吸收协议、查询、Workflow和UI逻辑，最终无法维护。

### 正确规则

1. Chat产品边界内固定两类核心能力：Chat Harness负责产品语义、事实与治理；Chat AI Runtime以
   MAF为运行基线，负责Agent、Workflow、模型和Tool协作。前端横跨两者提供交互，执行层是受
   RunSpec约束的独立运行边界。
2. Harness内部按状态所有权继续拆分Conversation、Work、Knowledge、Protocol、Context、
   Governance、Evidence和Delivery；“Harness”不是建立万能Service的理由。
3. 协作协议、ContextPackage、ExecutionDraft、RunSpec和StepInputProjection必须有独立版本与
   Hash；同一语义通过投影共享，不复制成多个可编辑事实。
4. 用户面板只提交选择、排除、修改或授权命令；刷新后从Product Store重建，不把浏览器状态当权威。
5. Agent和执行Runtime只能返回结构化候选、结果、Evidence或Product Patch；正式状态由Harness
   Application Coordinator经过Schema、权限、HITL、CAS和事务门提交。
6. 新能力进入明确模块与合同；不得继续堆入现有大型Harness/Governance Service或页面组件。

### 强制检查

- “Chat Harness”能否脱离某个Prompt、MAF版本和前端页面仍保持同一产品语义？
- 用户、Chat Workflow和执行层看到的协议是否引用同一revision，而不是三份复制文本？
- 前端刷新或换Session后，所有选择和状态能否从权威事实恢复？
- Agent返回“已完成”时，是否仍需Evidence与产品提交门才能改变Work状态？
- 新代码是否按状态、事务和失败所有权进入独立模块，而不是创建新的万能Harness类？

## 33. 反例 030：把手机适配缩成远程访问和聊天页响应式

### 错误触发

用户要求在手机端使用Chat产品，并提出本地服务器与远程访问入口后，方案只讨论VPN、云中转、
HTTPS、SSE重连和聊天输入框，没有逐项审计网页端已有或已确认的主页、连续对话、文件与Artifact、
Workflow、Project/Work、Harness、审批、配置和诊断能力。

### 错误本质

“手机可以访问服务器”是部署能力，“Chat Web在手机上完整可用”是产品能力。二者相互依赖但不能
互相替代。移动端不是桌面端的删减通道，而是同一Product Store、Product Session、Product Run和
Harness事实的另一种前端布局投影。手机屏幕更小只能改变导航、展开层级和编辑方式，不能让核心事实
或控制入口消失。

### 可能后果

1. 用户能发送消息，却无法查看执行产物、Workflow节点内容或Project进度。
2. HITL在手机上无法完整审核和编辑，用户被迫回到桌面才能继续Run。
3. 桌面和手机展示不同事实或不同版本，产生双状态和错误批准。
4. 团队用一个移动视口冒烟测试宣布“手机适配完成”，真实iOS键盘、锁屏、文件预览和长内容全部失效。
5. 网络层已经公开服务，但身份、敏感文件、缓存和移动设备丢失风险没有进入产品验收。

### 正确规则

1. 先建立“桌面功能 -> 移动入口 -> 移动交互形态 -> 权威数据 -> 降级规则 -> 测试证据”的完整矩阵。
2. Chat、主页/日历、Project/Work、Knowledge、文件/Artifact、Workflow、HITL、Context、配置和
   Trace都必须有手机入口；尚未实现的目标能力也必须保留目标位置，不能因为桌面端尚未完成而删除。
3. 手机采用单主视图与分层导航：桌面Workbench可以转为全屏详情、底部抽屉或独立路由，但关闭视图
   不取消Product Run，返回后从服务端权威状态恢复。
4. 文件和Artifact按类型提供可读预览、来源、版本、下载/分享边界；不支持的编辑能力必须明确只读，
   不能空白或静默失败。
5. 移动端与桌面端共享同一REST/AG-UI合同、版本Hash、CAS和授权；不得建立“移动专用事实”或在
   浏览器本地复制Harness状态。
6. 完成标准同时包含真实手机浏览器、软键盘、安全区、旋转/分屏、弱网/切网、后台恢复、跨设备冲突、
   大文件/长Workflow和可访问性证据。

### 强制检查

- 用户只拿手机，能否从主页重新定位并完成一次“查看Project -> 继续对话 -> 审核Context/Prompt ->
  观察Workflow -> 查看Artifact/Evidence -> 确认状态回写”的完整闭环？
- 桌面端每个一级能力是否都在移动功能矩阵中有入口、重排方式或明确的受审降级，而不是遗漏？
- 手机批准后，桌面是否立即看到同一Decision、revision和Run状态，反向操作也一致？
- 锁屏、切网或关闭Workbench后，Product Run是否继续且重新打开可以恢复？
- 自动化移动视口测试之外，是否至少有真实iOS Safari和Android Chrome证据？

## 34. 反例 031：把用户执行看护和个人主页冒充超级管理员运营看护

### 错误触发

产品已经设计Workflow Run View、个人主页、协作日历和Project/Work工作区，于是把“执行可看护”
理解成看护能力已经完整，遗漏超级管理员需要回答的“谁登录了、使用了多久、正在推进什么、作品到
什么状态、哪些用户或事项需要关注”。

### 错误本质

混淆了3种不同视角：

1. 普通用户看护自己的Agent、Workflow、Tool和结果。
2. 普通用户通过个人主页回顾自己的协作、工作和产物。
3. 超级管理员在经过授权和审计的运营看护台中查看全体用户的登录、使用、Project/Work和Artifact
   进展。

前两者不能替代第三者。OpenTelemetry、HTTP耗时、Provider耗时和Worker心跳也不能回答人的使用
情况；登录凭据有效时长、浏览器前台活跃时长、发生有效产品行为的协作时长以及模型/Tool运行耗时是
4种不同事实。

### 可能后果

1. 产品上线后无法知道真实用户是否进入、是否持续使用以及在哪里流失。
2. 超级管理员只能读取数据库、日志或猜测Token消耗，既不准确也不可审计。
3. Project百分比和Artifact状态被运营页面重新计算或手工维护，形成第二事实源。
4. 浏览器崩溃、网络中断或忘记退出被误算成持续活跃，使用时长严重失真。
5. 超级管理员权限被实现为直接读取全部内容，越过最小披露、隐私和访问审计。

### 正确规则

1. 超级管理员是受Identity授权的高权限Principal角色，不是配置开关、固定用户名或数据库直连者。
2. Identity拥有登录会话和认证事件；运营看护能力拥有活动采集、时间口径、聚合投影和管理员访问审计；
   Project/Work进度仍由Product Harness拥有，Artifact/Evidence状态仍由Evidence模块拥有。
3. 至少分别展示认证会话时长、前台活跃时长、有效协作活动和Run/Provider/Tool耗时，并公开计算口径、
   数据新鲜度与缺失状态。
4. 在线状态只能按最近可信活动表达为“活跃、可能离线、已过期或未知”，不能把没有退出事件冒充在线。
5. 超级管理员看护台默认读取必要元数据和权威状态；查看敏感正文、执行管理动作或导出必须有独立权限、
   原因和审计记录。
6. 运营投影可重建、允许短暂延迟，但不能成为用户身份、Work完成或Artifact有效性的写入入口。

### 强制检查

- 超级管理员能否按用户和时间回答谁登录、最后活跃、三类使用时长及数据置信度？
- 某个Project、Work或Artifact的进度能否回到唯一权威对象、Evidence和更新时间？
- 页面是否把HTTP/模型耗时、Token消耗或Worker心跳误称为用户使用时长？
- 普通用户是否绝对无法调用超级管理员查询，超级管理员的每次敏感查看是否可审计？
- 心跳丢失、重复事件、投影延迟和来源服务不可用时，页面是否显示陈旧/未知而不是伪造精确数字？

## 35. 反例 032：用进程内Gateway注册表却忽略后端进程归属

### 错误触发

pi子进程由一个后端进程启动，Provider Gateway又只通过该进程内的随机凭据注册表识别执行；重启时
旧后端仍有活动Run、新后端已经接管监听端口，pi请求因此到达没有对应注册表的新进程并返回401。
如果只看HTTP状态，会把本地进程归属错误误判为DashScope或模型密钥错误。

### 错误本质

进程内Future、RPC请求、子进程句柄和随机Gateway凭据具有明确的进程归属。持久ToolExecution与
MAF Checkpoint能保存稳定引用，却不能让另一个OS进程自动拥有这些内存对象；本机HTTP端口也不保证
请求会回到创建执行的进程。在线同进程重挂接与跨进程恢复是两种不同保证。

### 正确规则

1. 当前单实例部署在真实pi验证前必须确认只有1个后端进程监听并拥有Runtime；重启时把旧活动执行
   收敛为中断，不能让新实例冒充原进程继续。
2. pi注册、Gateway认证和注销日志必须记录ToolExecution ID、进程ID和不可逆凭证指纹，不记录原始
   Token或完整Payload；401诊断先比较进程与活动注册表，再检查上游Provider。
3. MAF Executor Checkpoint只保存稳定ID；同一进程可重挂接原Future/RPC边界，缺少活动进程时同时
   关闭Product Run与ToolExecution，不能遗留假`waiting_human`。
4. 真正的多实例或滚动重启续跑必须进入F05：增加可路由的执行所有权、进程间Gateway定位和副作用
   对账后单独验证，不能把当前同进程保证外推为跨进程保证。

### 强制检查

- 真实pi回合开始前是否只有1个预期后端实例，Gateway认证日志中的进程ID是否与注册日志一致？
- 本地401能否从日志区分“无活动执行、凭据不匹配、路由到错误进程”和“上游Provider拒绝”？
- 丢失进程所有者时，Product Run与ToolExecution是否一致失败关闭且没有重复Provider/Tool调用？
- 文档和前端是否明确区分同进程Checkpoint重挂接与跨进程活动执行恢复？

## 36. 反例 033：把抽象口号当成执行层可复用经验

### 错误触发

一次代码审查发现Kimi在SD4-A漏掉Claim Hash绑定字段、使用未验证的
`pre_commit_validity_check_passed=true`占位成功，于是只把“Hash要完整”、
“未实现能力必须fail closed”等口号写入下一次Prompt。执行层不知道具体哪些
字段影响后果、什么叫“假成功”、哪些场景可以例外，只能按自己的语义补全。

### 错误本质

抽象原则只压缩了结论，没有传递判断过程。执行层真正需要的是：

1. 什么条件下该经验才适用。
2. 项目中真实发生过什么错误，产生了什么后果。
3. 错误实现和期望实现的可对照案例。
4. 如何用测试、Hash变异、跨Scope攻击或质量门证明已满足。
5. 哪些是不可放宽的产品不变量，哪些只是当前情境下的建议。

没有这些内容，“经验”会变成另一份模糊需求；经验越多，Prompt越长，模型
反而越容易机械套用或忽略真正风险。

### 正确规则

1. 执行层经验使用“经验卡”：适用条件、想保护的目标、真实反例、正例、
   自检问题、确定性验证、例外和来源必须同时存在。
2. 硬不变量只保护权限、事实、事务、版本、Hash和完成语义；实现组织、
   类名、算法和局部抽象默认留给执行层自主判断。
3. 经验按任务风险标签选取；本次不涉及Hash、泛型引用或状态提交时，不加载
   对应经验卡。
4. 一次问题先记为候选经验；高影响或重复出现后再升级。经验必须可修订、
   可停用，不因模型名称永久化为“Kimi一定会犯”。
5. 执行后用首轮质量门通过率、高影响缺陷数和返工轮数判断经验是否有效；
   只增加上下文而没有改善结果的经验应修订或停用。

### 强制检查

- 执行层能否从经验卡中看到本项目的真实错误代码或状态变化？
- 经验是否同时提供错误后果、正确结果和可执行验证？
- 是否明确了“必须满足”与“建议优先”，而没有把全部经验写成硬规则？
- 本次StepInput是否只加载了与当前风险相关的经验卡？
- 经验的来源、信心、适用版本和停用条件是否可见？

## 37. 反例 034：用全局pi配置判断Chat执行层是否配置

### 错误触发

看到终端能够运行`pi`，就认为Chat执行层一定继承了它的全局模型、认证、Session和Tool；或者检查
`~/.pi/agent`没有发现目标Provider后，直接断言Chat里的pi没有配置。升级全局pi后，也没有核对Chat
自报的运行时合同版本是否仍与实际二进制一致。

### 错误本质

安装文件身份、进程实例、运行配置和产品执行事实是4个不同层次。同一个`cli.js`可以由终端和Chat
分别启动；Chat还会用临时`PI_CODING_AGENT_DIR`、短期Gateway凭据、隔离模型目录和`--no-session`
建立独立运行边界。反过来，即使Chat引用同一文件，版本更新也可能让固定RPC合同与真实安装版漂移。

### 正确规则

1. 先比较系统`command -v pi`和Chat配置`cli_path`的realpath、inode及`--version`，回答是否同一安装。
2. 再核对Chat启动参数、`PI_CODING_AGENT_DIR`、临时`models.json`、Provider Gateway和Tool Gate，回答是否共享配置与权限。
3. Chat采用哪个模型以Product DB Tool Profile Revision、公开Provider目录、ModelCallDraft和真实ToolExecution为准，不能以全局pi模型列表代替。
4. 运行时合同版本必须与实际安装版一致；升级后按依赖手册验证JSONL RPC、Tool拦截、逐次Provider审批和退出语义。

### 强制检查

- 当前是在问“同一个安装文件”，还是“同一个进程、Session、配置或授权”？
- 全局pi能否使用某Provider，是否被错误外推成Chat Tool Profile已经选择它？
- Chat真实Run能否证明使用了哪个Profile Revision、Provider、模型和Gateway？
- 实际`pi --version`、Chat健康投影和升级文档是否一致？

## 38. 反例 035：数据库测试依赖其他测试的模型导入顺序

### 错误触发

新增Artifact Store测试时，单独运行测试文件会在`create_all()`时报外键目标表不存在；把它与
Evidence服务测试一起运行却全部通过。原因不是Schema本身随机失效，而是后一个测试文件碰巧先导入
了Product Run、Repository Snapshot等模型模块，使共享`Base.metadata`变得完整。

### 错误本质

SQLAlchemy声明式模型注册依赖Python模块导入。测试收集顺序不是Schema依赖管理机制；如果一个测试
只在其他测试先导入隐含模块时成立，它验证的是进程偶然状态，不是被测模块的真实合同。

### 正确规则

1. 创建测试数据库的测试模块或共享Fixture必须显式导入其Schema外键所依赖的模型模块。
2. 新数据库能力至少分别运行单测试文件、相关测试组合和全量测试；三者必须得到同一Schema。
3. 不用修改测试顺序、重复定义Base或删除外键来掩盖注册缺失；生产迁移仍是部署Schema的权威路径。
4. 若多个测试反复需要完整Product Store模型，建立有明确职责的集中注册入口，而不是依赖通配导入。

### 强制检查

- 新测试文件能否在全新Python进程中单独运行？
- `Base.metadata`中的外键目标是由当前Fixture显式注册，还是来自其他测试的副作用？
- 随机测试顺序、定向测试和全量测试是否产生相同表集合与约束？
- 修复是否保留了生产外键和迁移合同，而没有为了测试方便降低Schema完整性？

## 39. 反例 036：把虚拟环境启动器和真实解释器路径混成一个沙箱根

### 错误触发

确定性Validation Runtime把`.venv/bin/python`先做`resolve()`，再把结果作为唯一可读路径交给
macOS seatbelt。uv创建的虚拟环境启动器实际指向用户目录下的独立Python发行版；解释器虽然能被
找到，却无法读取自己的标准库或`pyvenv.cfg`语义，真实沙箱验证失败。

### 错误本质

虚拟环境启动器路径表达“使用哪个环境”，解析后的解释器路径表达“机器代码和标准库实际在哪里”。
两者都属于执行环境指纹，但承担不同责任。只保留前者会缺运行时文件，只保留后者会丢虚拟环境语义；
直接开放整个Home目录又破坏了最小权限。

### 正确规则

1. 编译和启动保留项目`.venv/bin/python`路径，不静默回退系统Python，也不提前解析掉虚拟环境语义。
2. 环境指纹同时绑定启动器、解析后的解释器、锁文件和关键项目配置；执行前重新校验。
3. OS沙箱只读放行项目Workspace、虚拟环境、解析出的解释器发行目录和必要系统运行时；写权限只给
   本次Validation临时目录，网络默认拒绝。
4. 必须用真实沙箱证明三件事同时成立：项目测试可运行、Workspace外敏感文件不可读、网络不可达。

### 强制检查

- 实际spawn使用的是项目虚拟环境启动器，还是被`resolve()`替换后的全局解释器？
- 沙箱放行的是精确Python发行目录，还是为了省事开放整个用户目录？
- executable、environment和argv Hash是否在spawn前重新计算并一致？
- 真实OS沙箱测试是否同时覆盖允许路径、拒绝路径和拒绝网络？

## 40. 反例 037：只校验新完成投影，却让旧Evidence继续写完成事实

### 错误触发

SD4-C为Harness完成状态增加了`result_commit_id + claim_id`保留投影校验，但实现只在Evidence
包含保留字段时进入校验。调用方继续提交任意旧Evidence或`completion_waiver_reason`时，代码直接
跳过Result Commit Gate，仍能把Work/Action改为`completed`；测试还把这条旧写路径当成正确行为。
同一实现又把公开可构造的`ClaimGateRecheck`称为“只有Coordinator能创建”，记录层只比较部分字段，
使伪造收据可以把候选Claim写成已提交事实。

### 错误本质

兼容读取与兼容写入是两件事。新的权威完成门上线后，旧字段可以继续展示历史记录，但不能继续成为
授权入口。保留字段检测只能识别一种伪造形状，不能证明没有其他旁路；应用内可构造的数据对象也不是
权限能力，任何下层记录方法都必须把它视为不可信声明并复核权威行。

### 正确规则

1. 所有通向`completed`的公开写路径统一fail closed：必须是唯一ResultCommit/Claim投影、携带CAS
   版本并通过同事务链校验；旧Evidence和旧waiver只读兼容。
2. 完成门测试必须从结果反推全部入口，至少攻击无Evidence、任意旧Evidence、旧waiver、混合投影、
   多投影、无校验器和已消费引用，不能只测试新保留字段。
3. Receipt、DTO和冻结dataclass都不是capability。成功写入还必须消费Coordinator在当前事务签发的
   一次性opaque nonce；记录层随后重新查询Claim版本、mandatory集合、Adoption/Waiver解析、
   Artifact id/version、Decision精确绑定和目标状态，不能信任调用方布尔值或ID集合。
4. 阶段完成声明必须同时核对批准范围和主链接线；独立REST端点存在不等于“已接入主Workflow”。

### 强制检查

- 是否仍有任何HTTP、Application Service、Participant或测试Fixture可绕过Result Commit写`completed`？
- “legacy兼容”是否明确限定为读取，而不是继续接受旧格式写入？
- 下层记录方法面对人工构造的Receipt时，能否从Product Store独立重建并比较全部关键事实？
- 项目状态中的“阶段完成”是否逐项对应批准设计的交付表，而不是只对应某个新类或端点？

## 41. 反例 038：把TUI的thinking流误判为执行层没有调用Tool

### 错误触发

pi Agent使用Kimi K3执行有界修复时，TUI持续滚动显示thinking。协调者没有读取持久Session事件，
就把可见thinking误判为“没有Tool Call”，中断3个仍在工作的会话并自行接管生产代码。事后JSONL
证明第1个K3会话已有58个Tool Result，第2个K3会话已有10个Tool Result；新的交互式K3冒烟也真实
产生`read` Tool Call和Tool Result。

### 错误本质

执行层是由模型、Provider适配、流事件解析、Tool注册、权限模式、Session上下文和终端调用共同组成的
运行合同。TUI显示的是运行投影，不是Tool Ledger或终态；协调者接管还会掩盖真实进度与执行链故障，
让后续任务继续在同一失效基础上运行。项目已经选择pi作为执行层时，协调者的责任是提供有界工作包、
诊断和恢复Tool Loop、审查操作与独立验收，而不是在首次异常后替代执行层。

### 正确规则

1. 冻结并保留当前工作树，先在无项目上下文的临时目录运行最小Tool冒烟：只注册1个只读Tool，要求
   Agent调用1次并返回固定标记；不得用复杂项目Prompt直接判断Tool Loop健康。
2. 依次核对实际pi版本与realpath、CLI参数、已注册Tool列表、模型与思考模式、Provider流事件类型、
   `tool_call`增量、停止原因、Session事件记录和输出解析；不得读取或打印密钥与完整Provider Payload。
3. 使用同一Prompt对比Kimi K3的不同思考模式和一个已知可工作的Provider/模型，区分模型行为、
   Provider适配和pi核心Tool Loop；对比模型只用于定位，正式任务仍使用用户指定的执行组合。
4. Tool Loop恢复后用全新Session和有界步骤重新派发；pi负责读取、修改和定向测试，协调者负责操作
   审查、Diff审查、攻击测试与全量验证。发现缺陷继续交回同一执行层修复，不能静默接管。
5. 只有形成可复现证据，证明pi安装、Provider适配或权限链当前确实无法执行，并且修复需要超出本项目
   授权范围时，才可暂停并向用户报告；“输出很长”“暂时没改文件”不构成接管理由。

### 强制检查

- Agent是否实际收到了预期Tool Schema，Session记录中是否出现`toolCall/toolResult`或明确的终止原因？
- 最小只读Tool冒烟是否通过；如果不通过，是否已区分Kimi K3、Provider适配与pi核心问题？
- 是否错误地把可见thinking增量当成最终回答，或把仍在运行的Agent提前判定为失败？
- 正式任务是否使用全新Session、有限上下文、明确完成条件和必要Tool Allowlist？
- 协调者发现质量问题后是否把修复交回pi，并保留独立测试与检视责任？

## 42. 反例 039：用 `rm -rf` 批量清理被数据库引用的工作区目录

### 错误触发

SD4-C开发期间，为了“清理测试产生的临时工作区”，开发代理对
`backend/.data/execution-workspaces/`执行了通配`rm -rf`（保留其中一个目录）。
该目录是Execution Workspace服务的受管根，不是临时测试根：产品DB中每条
retained Workspace记录的workspace_key都指向其中一个子目录。

### 错误本质

文件系统清理与产品事实所有权分离。shell层面的“看起来是测试残留”不等于
可以删除：ExecutionWorkspaceRecord（尤其`retained`终态）是对该目录的权威
引用，DB与磁盘的一致性只能由拥有该关系的Workspace服务判定，批量通配删除
把“清理”变成了无记录的产品数据破坏。

### 实际后果

产品DB仍有4条`retained` Execution Workspace记录，磁盘只剩1个对应目录，
3个被误删；删除不可由shell恢复，DB未删改、未尝试恢复，处置只能交回用户。

### 正确规则

1. 禁止对任何非临时测试根使用`rm -rf`、`rm -r`、通配清理或“保留一个其余全删”。
   拿不准某个目录是不是临时测试根时，一律视为产品数据。
2. 删除任何Workspace目录前，必须由Execution Workspace服务（而非shell）逐条校验：
   DB记录ownership、状态已是可删除终态（非retained/非活动）、路径确在受管根内；
   当前没有这样的服务接口时，就不删。
3. 测试产生的目录只能创建在`tmp_path`等测试专属临时根内，并由测试框架自行回收；
   生产/开发数据根的清理需求先写成服务级任务并交用户审核。
4. 数据库被引用的目录绝不靠shell批量删除；需要回收磁盘时，先做DB记录盘点与
   用户确认，再由服务执行带审计的逐条处置。

### 强制检查

- 本次要删除的目录是否被任何DB表（Execution Workspace、Artifact、Run数据）引用？
- 删除操作是否经过拥有该关系的服务，并校验了终态与路径边界？
- 测试残留是否全部落在`tmp_path`内，不需要进入开发数据根？
- 是否把“保留一个目录”误当成安全网，实际上其余被删目录同样有权威引用？

## 43. 反例 040：把一次验证残留包装成需要用户保留的“证据”

### 错误触发

为了证明SD3真实隔离写入和Kimi Provider接入可用，开发者分别留下了一个带未提交修改的受管
Execution Worktree和一个正式Product Store中的“Kimi验证会话”。验证已经完成后，协调者仍把两者
描述成“刻意保留的证据”，并要求用户决定何时取证和清理。

### 错误本质

运行时验证结果与验证过程产生的临时载体不是同一件事。真正有长期价值的证据应该被项目正式消费：
自动测试固定合同，Trace或Artifact保存可追溯结果，项目状态只记录必要结论。脏Worktree和测试会话
既没有进入交付链，也会污染工程与真实产品数据；把它们留给用户决定，只是在转移创建者的清理责任。

### 正确规则

1. 开始真实验证前必须确定证据落点和清理路径；能在测试专属Store、临时Workspace或独立Scope完成
   的，不写入正式开发数据。
2. 有长期价值的结果必须进入自动测试、Product Trace、Evidence/Artifact或拥有该事实的正式文档，
   并能说明项目如何查询、回归或审计它；仅仅“目录还在”或“会话还在”不构成价值。
3. 没有长期价值的Worktree、测试Product Session、Run、临时文件和进程由创建验证的开发者在本轮
   收口；不得默认保留，不得把是否清理交给用户。
4. 清理仍须遵守事实所有权：受管Workspace通过拥有它的服务逐条处置，Product Session通过产品
   生命周期或受控运维路径删除；禁止用通配shell删除数据库引用的数据。
5. 如果产品尚无必要的删除能力，应明确这是生命周期缺口，并使用保持引用完整性的最小受控处置；
   不能用“归档”等不同语义冒充删除，也不能因此永久保留测试垃圾。

### 强制检查

- 这份所谓“证据”具体由哪个测试、Trace、Artifact或项目事实消费？没有消费者就不保留载体。
- 验证是否污染了主仓库、正式Product Store或用户可见会话列表？
- 创建验证数据的人是否已经在同一工作包中定义并执行清理？
- 删除受管数据时是否经过所有者服务并保持数据库、文件和关联事实一致？
- 用户要求“删除”时，是否真实删除而不是擅自改成归档或继续要求用户确认？

## 44. 反例 041：用营销页面和功能清单冒充外部产品深层研究

### 错误触发

研究类LifeOS产品时，主要读取官网、README、Marketplace和少量讨论，再依据术语相似度给出“最接近Chat”、
“自动维护能力很强”等结论；没有固定源码提交，没有还原安装、数据写入、Agent循环、记忆、升级和失败链路，
也没有实际运行或检查测试与Issue。用户因此只能看到产品定位摘要，无法知道系统实际做了什么、是否有效以及代价。

### 错误本质

产品公开叙述、代码实现、可运行行为、长期用户效果和社区口碑是5种不同证据。README中的对象名称不证明它有
稳定状态机，代码文件存在不证明主链路会调用，演示成功不证明真实用户能持续维护，Star和单条好评也不证明留存。
如果不逐层核对，研究只会把作者愿景重新措辞，不能支持Chat的采用、改造或拒绝判断。

### 正确规则

1. 源码可得的项目先记录仓库URL、固定提交、Release、许可证、运行依赖和公开源码边界；进入仓库前读取其规则文件。
2. 围绕具体问题还原组合根、核心对象、状态载体、写入时机、主调用链、失败处理、升级/迁移和测试，而不是整仓泛读或按目录名猜能力。
3. 安全可行时在隔离目录执行安装、Doctor、核心命令和代表性场景；记录输入、实际文件/状态变化、日志、失败和清理方式。没有运行条件时明确标为“源码事实，未实测”。
4. 用户效果与口碑分开采样官方Issue/Discussion、Release修复、论坛、Reddit、Marketplace、YouTube评论和独立评测；记录样本量、时间、是否付费用户及偏差，不能从单条评价外推频率。
5. 结论使用统一证据标签：`文档宣称`、`源码事实`、`测试事实`、`运行实测`、`用户报告`、`项目推断`。相互冲突时公开冲突，不用较弱证据覆盖较强证据。
6. 最终逐项给出Chat的采用、改造和拒绝项，并说明参考项目真正覆盖与未覆盖的范围；概念相似度不能自动升级为正式架构参考。

### 强制检查

- 是否能从真实入口追踪一次用户动作到状态写入、Agent/Tool执行、结果呈现和下次恢复？
- 每个“已经做到”的判断是否至少有源码/测试或运行证据，而不只是作者文案？
- 是否区分安装成功、一次演示成功、持续使用有效和大众口碑成熟？
- 口碑结论是否说明样本来源、数量、时间和证据强弱？
- 无法取得Better Creating等商业模板内部结构时，是否明确停止在可验证边界，而没有编造数据库Schema或公式？

## 45. 反例 042：让同步App Shell依赖懒加载页面的CSS

### 错误触发

生产Home加入全局`ActivityRail`后，导航组件由`App.tsx`同步渲染，但它的全部样式仍放在
`home.css`，而`home.css`只由懒加载的`HomeView`导入。用户停留在Chat视图并刷新时，应用依据
`sessionStorage`直接恢复Chat，`HomeView`没有加载，导航因此退化为浏览器默认按钮并横向挤压
Session侧栏、Chat Pane和Workflow Workbench。既有E2E总是先打开Home再切到Chat，恰好先加载了
CSS，所以没有发现真实刷新路径。

### 错误本质

代码加载边界和视觉所有权不一致。同步存在的App Shell不能把布局成立条件寄托在某个可选页面Chunk；
“从默认页切换成功”也不能证明深链接、状态恢复或整页刷新后仍加载相同资源。

### 正确规则

1. App Shell、全局导航和跨页面共享组件的必要样式由组件自身或同步入口导入；页面专属CSS继续留在
   真实懒加载边界，不能为修复问题把整页样式重新塞回主包。
2. 每个持久页面状态至少验证一次冷启动或整页刷新，不依赖先访问默认页面产生的CSS/JS副作用。
3. 回归测试同时断言关键布局合同（方向、宽度、可见性）和页面无横向溢出；只看元素存在不足以发现
   浏览器默认样式退化。
4. 生产构建继续检查CSS分包：共享壳样式进入同步Chunk，重型页面样式仍保持按需加载。

### 强制检查

- 当前同步渲染的组件是否只在某个`lazy()`页面模块中导入CSS？
- 从非默认视图整页刷新后，关键导航的计算样式和尺寸是否仍正确？
- E2E是否先访问了会掩盖资源缺失的默认页面，导致测试顺序依赖？
- 修复后是否保持移动端媒体规则、页面CSS按需加载和无横向溢出？

## 46. 反例 043：用少量Docstring和内部事件冒充可掌握的完整Workflow链路

### 错误触发

持续协作主Workflow已经有模块级39节点清单、少数Executor Docstring和Product Trace，项目状态便写成
“完整链路补充代码注释”。但用户打开`collaboration_intents/service.py`、Workflow协调器、pi执行、
治理、结果提交或持久化函数时，仍然无法就地判断它属于哪个阶段、对应哪个节点、接收什么、为何选择
这条路、字段为什么为空以及结果交给谁。一次Run结束后也只有分散的数据库行、节点Trace和进程日志，
需要人或Agent临时拼接，不能直接得到一份稳定、可复查的流程说明。

### 错误本质

“代码有注释”“运行有事件”“用户能掌握链路”是3种不同保证。模块概览只提供地图，不能代替关键函数
附近的定位信息；原始诊断事件服务机器排障，不能直接承担人类教学；让Agent事后总结又会把缺失事实、
当前代码推断或隐藏推理混进历史。真正可还原的Workflow需要在执行当时保存结构化事实，并由确定性
投影器从同一事实源生成两种用途不同、可以交叉核对的Trace产物。

### 正确规则

1. 持续协作主链的入口、应用服务、39节点Executor、分支条件、治理、pi/Tool、Evidence、最终提交和
   Trace查询必须建立统一的中文代码导读标记；关键类或函数就地说明阶段/节点、输入、输出、状态所有者、
   不变量、失败/恢复语义和下游。注释解释业务角色与原因，不逐行翻译Python语法。
2. `pi Session`、Product Session、Product Run、Workflow Checkpoint和ToolExecution分别说明是否
   持久、保存什么和不保存什么；禁止用“有Session”笼统回答历史、恢复或审计能力。
3. 每个进入终态的Product Run确定性物化两份同源产物：机器诊断Trace保留版本化结构事件、关联ID、
   状态转换、耗时、错误和证据引用；人类流程报告按阶段/节点解释实际路径、选择依据、未走分支、空值
   原因、审批结果、模型/Tool次数、提交结果和未完成保证。
4. 两份产物必须来自Product Trace、Workflow Definition、Decision、Attempt、Tool/Evidence和终态等
   权威结构化事实，不调用模型生成，不保存隐藏推理，不拿当前代码反推旧Run；历史字段缺失时明确写
   `not_recorded`及原因。
5. 每个路由和可选字段使用稳定Reason Code加人类文案。`空`至少区分not_applicable、not_selected、
   not_produced、redacted、failed_before_production和historical_not_recorded，不能只输出`null`。
6. 两份Trace都绑定`product_run_id + workflow_id/version + trace_schema_version + generated_at`和来源
   sequence范围；Workflow恢复、Outbox重投或投影重建必须幂等得到同一语义结果。
7. 可视化只读取人类流程报告和Definition拓扑，把实际节点、边、原因、审批和证据叠加显示；前端不得
   重新猜路径，也不得成为Trace事实源。
8. 面向项目掌握的材料不能以“阶段A、节点3、某字段会被包装”作为第一解释层。首次出现的对象必须
   先用一个具体用户输入展示它从哪里来、解决什么问题、若不存在会怎样、为何保存或不保存、在内存/
   数据库/协议中的具体形态，以及如何通过断点、数据库查询和Trace亲眼验证；随后才能展开节点和类。
   代码注释只负责就地解释职责与不变量，不能替代这条可执行学习链。
9. 一个讲透的样例不能冒充“全盘掌握”。项目掌握索引必须同时维护3套可对账清单：完整目标产品的
   模块/场景/未实现能力、当前仓库的全部源码模块/进程/协议/Store/测试、主Workflow全部节点与分支。
   每一项都要映射到学习单元、当前资料、调试或验证方式和L1-L3掌握状态；任何未覆盖项必须显式标为
   待补，不能因为当前尚未实现、未进入主Workflow或没有独立目录而从总纲消失。

### 强制检查

- 从用户点击发送开始，能否沿中文注释逐步跳到入口、Context、Intent、Plan、授权、pi/回答、Evidence、
  提交和最终Message，而不依赖聊天中的临时解释？
- 任意终态Run是否都有两份同源、可定位、可重建、Schema版本明确的Trace产物？
- 人类报告中的每个“因为”“未走”“为空”能否定位到结构化Reason Code、输入事实或Decision记录？
- pi关闭`--session`时，文档和代码是否诚实说明它没有pi自身的跨调用Session历史，而不是用Product Trace
  或Checkpoint冒充？
- 旧Run缺少分支求值或字段原因时，报告是否明确标记历史未记录，而不是按当前Definition推断？
- 第一次接触某个字段的人，能否沿“具体例子 -> 对象来源 -> 代码符号 -> 数据库行 -> 后续消费者”
  亲手验证，而不是只能背诵阶段和节点名称？
- 学习材料是否给出明确断点、应观察的变量、无敏感正文的数据库查询和复述题，能证明读者已经掌握？
- 项目掌握总纲能否逐项覆盖11个目标产品模块、全部当前源码能力、进程/存储/协议边界、39个主Workflow
  节点、测试/部署/运维和所有未实现目标，并能从任一项反查资料与实验？
- 是否把一个讲得很细的Context或阶段A样例误写成“体系已经全面”，而没有给其余能力分配明确学习单元？

## 47. 反例 044：把跨仓Source Map解析冒充可用的源码调试体验

### 错误触发

Chat和pi是两个独立仓库，却把`Attach pi Source Runtime`放在Chat的VS Code工作区，并通过绝对
`outFiles`、`resolveSourceMapLocations`指向pi仓库。配置在技术上可能解析Source Map，随后便宣称
用户可以正常进入pi源码下断点；但用户实际只打开Chat项目，pi源码树、断点管理、构建任务和Git上下文
都不在当前源码窗口中。

### 错误本质

“运行时使用哪份二进制”“调试适配器能否解析Source Map”和“开发者在哪个源码工作区设置、保存和
命中断点”是3个不同保证。独立仓库应保持各自的源码调试所有权：调用方只负责让子进程暴露Inspector，
被调试仓库负责源码编译、断点与Attach。把外部源码路径塞进调用方工作区，会让配置看似一键完成，
实际破坏开发者的仓库心智模型。

### 正确规则

1. Chat工作区只启动Chat前后端，并在专用调试模式下让其派生的pi进程使用固定回环Inspector端口；
   不在Chat窗口Attach pi源码。
2. pi工作区提供自己的构建Task和Node Attach配置；`outFiles`、Source Map和断点路径全部以pi的
   `${workspaceFolder}`为根。
3. 推荐顺序是：先在pi窗口启动“构建并等待Attach”，再在Chat窗口启动外部pi调试模式，最后触发
   一条实际进入pi执行分支的Run。`inspect-brk`保证Attach前不越过首段用户代码。
4. 普通Chat启动可以继续使用已编译的同一pi源码产物，但不承诺自动重编译或命中pi断点；这两项只由
   专用双窗口调试流程保证。
5. Chat调试结束必须恢复普通pi运行配置，避免后续非调试Run因等待外部调试器而卡住。

### 强制检查

- pi断点是否在打开pi仓库的VS Code窗口中创建，而不是在Chat窗口临时打开外部文件？
- pi窗口是否拥有构建Task、Source Map根和Attach配置？
- Chat窗口是否只负责启用Inspector并启动子进程，而且停止后能恢复普通配置？
- 未真正进入pi执行分支时，是否诚实说明不会出现Inspector进程、更不会命中断点？
- 普通`Chat Full Stack`与双窗口源码调试入口是否在名称和行为上明确区分？

## 48. 反例 045：PWA缓存外壳打开后，把认证失效只显示成401

### 错误触发

PWA或已加载Web App Shell仍能从Service Worker缓存或浏览器内存显示，边缘Basic Auth凭据却已被
浏览器清理、替换或判定无效。REST和AG-UI请求开始返回`401 + WWW-Authenticate`，前端只显示原始
`401`、把它误称为后端离线，或反复重试`fetch`，没有提供能重新触发浏览器认证挑战的入口。

### 错误本质

“App Shell可以显示”“网络可达”“边缘认证有效”和“Product Authentication Session有效”是4种
不同状态。Service Worker可以让界面在没有有效凭据时继续打开；后台`fetch`收到401也不等于用户
已经获得可执行的登录流程。Basic Auth只是当前部署的边缘访问门，更不能冒充正式Product Identity。

### 正确规则

1. REST与AG-UI必须经过同一个前端HTTP边界识别401，统一进入“需要重新认证”，不能由20个Feature
   分别把它显示成普通请求失败。
2. 重新认证必须使用顶层文档导航进入同源、同Realm的专用入口；该入口必须`no-store`、不进入PWA
   Precache，并从Navigation Fallback中排除，否则Service Worker会再次返回缓存App Shell。
3. 认证失效与离线、Relay中断、后端未启动和Product权限不足分别呈现；401恢复按钮不能声称已经
   建立正式Principal、Role/Grant或Authentication Session。
4. 重新登录只恢复传输凭据，再从Product Store和Runtime Journal重建权威状态；不得自动重放
   Provider调用、Tool副作用、写命令或不确定请求。
5. 自动验证至少覆盖REST 401、AG-UI 401、桌面/手机登录卡片、认证入口未预缓存、未认证挑战、
   认证后`no-store`和生产子路径构建。

### 强制检查

- PWA离线外壳能打开但API返回401时，用户是否看到“重新登录”而不是裸401或“后端离线”？
- 登录入口是否真的到达网络并触发文档级`WWW-Authenticate`，而不是再次命中Service Worker？
- REST和AG-UI是否都会触发同一恢复状态，且不会形成无限轮询或自动重放副作用？
- Basic Auth、正式Product Authentication Session和Product Session是否仍明确分开？

## 49. 反例 046：把LaunchAgent已加载和远端端口仍监听冒充中转健康

### 错误触发

反向SSH进程因网络异常退出并返回255，`launchctl print`仍能找到已加载的Job，云服务器上的旧
`sshd`会话也仍占用反向端口。验收脚本只检查Job可打印、端口存在或静态页可访问，便把中转写成
“运行中”。实际旧会话已经无法把请求送回本地，远端连接堆积为`CLOSE-WAIT`；launchd反复启动新
进程，又因旧端口仍被占用而持续报`remote port forwarding failed`。用户认证后最终看到502/504。

### 错误本质

“LaunchAgent已加载”“Relay进程正在运行”“云端反向端口有人监听”和“请求能够端到端到达本地
服务”是4种不同事实。客户端`ServerAliveInterval`只能帮助活动SSH客户端发现服务端失联；客户端
已经退出而服务端保留半开会话时，它不能替服务端清理旧`sshd`和释放反向端口。

### 正确规则

1. 守护进程验收必须同时检查`state=running`、有效PID和最近退出码；`launchctl print`成功只证明
   Job已加载，不能证明进程存在。
2. 反向端口验收必须请求真实健康端点并得到预期响应；`ss`显示LISTEN不能证明转发通道可用。
3. 发现“本地服务200、远端回环超时、Relay退出255、旧端口仍由`sshd: root`占用”时，只能按唯一
   服务端口精确核对监听进程与会话，再先TERM、必要时才KILL该条失效会话；禁止批量终止`sshd`。
4. 旧端口释放后再由对应LaunchAgent重建连接，并同时验证本地健康、远端回环、认证前401、认证后
   公网200和真实移动页面；Relay恢复不得重放Provider、Tool或产品写命令。
5. 长期自愈必须补充服务端半开SSH会话回收或等价的带所有权清理机制，并让监控区分loaded、
   spawn scheduled、running、stale listener和end-to-end healthy；在该机制实现前保留复发风险。

### 强制检查

- 验收是否读取了LaunchAgent运行态和PID，而不只是检查Label存在？
- 云端端口是否通过真实健康请求，还是只看到LISTEN就宣称可用？
- 新Relay反复退出255时，是否检查了旧服务端`sshd`会话和`CLOSE-WAIT`连接？
- 清理是否只针对已核对端口所有权的单个失效会话，没有影响其他SSH登录和服务？
- 恢复后是否分别验证pi-web与Chat两条中转，并明确没有重放任何执行副作用？

## 50. 反例 047：手写阶段口径漂移，并用节点清单冒充完整教材

### 错误触发

项目掌握文档分别出现“7阶段”“阶段A/B”“12个代码阶段”和“6条运行路径”等说法，却没有先说明
这些数字分别属于哪一个维度，也没有由当前Workflow Definition自动生成映射。部分文档仍引用旧版
`v1.4.0/28节点`，代码已经是`v1.8.0/39节点`；读者因此无法判断哪个才是当前事实。与此同时，材料
详细解释少数Context节点，却缺少应用启动、前端、协议、运行恢复、Evidence等模块的同等导读。

### 错误本质

产品交付阶段、可执行Workflow的真实节点、为了学习划分的阶段、节点内部代码阶段和一次Run实际经过
的分支路径是5个不同维度。手工维护数字和名称会随着代码演进漂移；只校对节点数量也不能证明架构、
模块、基础概念、例子、代码注释和调试实验已经形成可学习闭环。

### 正确规则

1. 主Workflow版本、节点ID、边和学习阶段归属必须由当前Workflow Catalog与Graph Factory导出一份
   机器事实；人读文档只解释，不另造权威数字。
2. 当前主Workflow学习阶段使用稳定`S1...S7`标识、中文名和节点范围；`阶段A/B`等历史称呼只能作为别名，并必须
   显式映射到稳定阶段，不能与项目`阶段0-8`或设计者代码阶段混写。
3. 每份Workflow文档首页标明自己使用的维度：产品交付阶段、学习阶段、MAF节点、节点内部代码阶段
   或实际运行路径。数字相同或不同都不能省略限定词。
4. 自动检查全部项目掌握文档中的Workflow版本、节点总数、学习阶段数、节点归属和名称；发现旧数字
   或未映射阶段直接失败。
5. 完整教材覆盖产品架构、模块、对象、协议、Store、进程、前端、Workflow、执行、Evidence、测试和
   未实现目标。每个单元都要有基础概念、具体例子、图、代码锚点、必要中文注释和用户可做实验；没有
   完成时必须显式登记缺口。
6. 代码注释解释节点/阶段位置、输入输出、状态所有者、设计原因、失败恢复和下游，不复述语法；注释
   与文档共同引用稳定符号和节点ID，避免靠易漂移行号定位。

### 强制检查

- 文档中的每个“阶段”是否带有限定词，并能映射到代码或计划唯一事实？
- 当前Workflow版本、节点数、边数、学习阶段名和节点归属能否由脚本重新生成并与文档一致？
- 是否仍有`v1.4.0/28节点`等历史口径被写成当前实现？
- 每个学习单元是否同时拥有概念、例子、图、代码入口、注释状态和实验，而不是只有标题？
- 新增或删除节点后，质量门是否会指出所有需要同步的学习文档？
