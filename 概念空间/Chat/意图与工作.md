# 意图与工作

## 文档治理信息

| 项目 | 内容 |
|---|---|
| 目的 | 区分AI理解到的目标、长期工作、计划结构和下一责任。 |
| 概念状态 | 有效；属于Chat稳定产品闭环。 |
| 实现状态 | 局部实现并验证：主Workflow已有Intent候选/修正、ExecutionDraft授权和授权后RunSpec编译；完整WorkItem/TaskPlan/ActionItem生命周期与独立编辑工作区未完成。 |
| 事实所有者 | 产品目标与对象见[项目上下文](../../PROJECT_CONTEXT.md#7-核心领域对象)，路线见[项目计划阶段4](../../PROJECT_PLAN.md#9-阶段-4上下文意图工作与执行门)。 |
| 维护责任 | Collaboration/Work模块详细设计时重入并补状态机。 |

## 一句话理解

**Intent说明系统认为用户想达成什么；WorkItem承载跨回合事项；TaskPlan描述怎样推进；ActionItem明确下一步由谁负责；ExecutionDraft让用户修改准备怎样执行，RunSpec则把已接受内容冻结为运行合同。**

## 为什么需要

如果把模型回答中的“你应该做X”直接当成任务，AI会替用户创建长期事实；如果把一次澄清、长期目标、计划步骤和下一动作都叫“任务”，用户无法判断系统理解了什么、已经接受了什么以及谁应该行动。

## 定义与边界

| 概念 | 定义 | 成为正式事实的条件 | 不是什么 |
|---|---|---|---|
| Intent / 意图 | 系统从输入和上下文观察到的目标、依据和不确定性 | 用户确认或明确产品规则接受某一版本 | 不是已执行命令或长期Work |
| Intent Candidate / 意图候选 | 模型或规则提出、等待检查的Intent版本 | 尚未正式生效 | 不是系统已经“知道”的事实 |
| WorkItem / 工作项 | 需要跨回合持续推进的工作或学习事项 | 经产品命令创建或采纳 | 不是聊天标题、单条Prompt或Run |
| TaskPlan / 任务计划 | 为一个或多个WorkItem组织的步骤、顺序、依赖和检查点 | 用户或策略接受某一版本 | 不是Workflow Definition或Runtime Job |
| Plan Node / 计划节点 | TaskPlan中的工作步骤或检查点 | 随所属TaskPlan版本生效 | 不是MAF Workflow Node |
| ActionItem / 行动项 | 明确由用户、AI或系统承担的下一行动 | 进入Work生命周期并具有责任人 | 不是泛泛建议或模型Tool Call |
| ExecutionDraft / 执行草稿 | 汇总目标、背景、已接受决定、范围、计划、Context引用、资源、能力、Prompt组装计划、验证和停止条件的可编辑产品草稿 | 经过有效HITL决定后可用于编译RunSpec | 不是一次Provider请求或已经开始的Run |
| RunSpec / 运行规格 | 从已接受ExecutionDraft、Context、权限和策略快照编译出的不可变执行合同 | 编译成功且所有引用仍有效 | 不是可在Worker中临场修改的Prompt或ModelCallDraft |

## 概念关系

```text
一次 Interaction
-> 产生零到多个 Intent Candidate
-> 确认后可创建或更新 WorkItem
-> WorkItem 可关联版本化 TaskPlan
-> TaskPlan 包含 Plan Node
-> 当前可执行责任形成 ActionItem
-> 需要运行时生成 ExecutionDraft
-> HITL策略解析并取得人工或自动决定
-> 编译不可变 RunSpec
-> 创建 Product Run / Run Attempt
```

TaskPlan是用户工作结构；Executable Workflow是系统执行图。两者可以映射，但不能因为节点名称相似就合并。

ExecutionDraft回答“准备怎样做”，允许用户直接修订；RunSpec回答“这次运行被授权按什么合同做”，编译后不可变；ModelCallDraft只回答Run内“这一次即将向某个Provider发送什么”。三者不能合并。

## 人和系统怎样使用

1. AI表达“我的理解”时展示Intent、依据和不确定性，不写成已经确认的目标。
2. 用户说“把它列为长期事项”才通过产品命令创建或采纳WorkItem。
3. 计划修改产生新版本；旧版本的执行批准不能自动覆盖新计划。
4. ActionItem必须显示责任人和完成判断，避免“我们下一步”这种无所有者表达。
5. 前端分别呈现意图审核、Work状态、计划和下一动作，不用一个聊天气泡代替全部对象。
6. ExecutionDraft按任务、背景、目标、已接受决定、范围、步骤、资源、能力、Prompt组装、验证、输出和停止条件展示；接受后编译RunSpec，任何语义变化都要产生新版本并重新判断HITL。

## 正例与反例

正例：系统显示“我理解你要完成Session恢复设计，信心0.8；是否作为长期WorkItem？”用户确认后才创建。

反例：模型说“下一步应修改数据库”，系统立即把WorkItem标记为进行中并启动Tool。

反例：把TaskPlan里的“审批模型请求”节点直接当成MAF Workflow节点ID，导致计划编辑改变运行图。

反例：用一条Assistant Message的存在证明ActionItem已经完成。

## 当前状态与未知

Intent、WorkItem、TaskPlan、ActionItem、ExecutionDraft和RunSpec是有效产品概念。当前持续协作主Workflow已能识别单个Intent及置信度、在歧义时请求澄清或允许用户修正，并持久化ExecutionDraft；只有当前Draft revision经人工或策略授权后，系统才编译并绑定不可变RunSpec。当前Intent仍是Workflow内候选而非独立长期资源，WorkItem/TaskPlan/ActionItem正式Schema、状态机、完成证据和完整前端审核仍未实现。

## 来源、维护与验证

来源：[完整产品闭环](../../PROJECT_CONTEXT.md#6-完整产品闭环)、[核心领域对象](../../PROJECT_CONTEXT.md#7-核心领域对象)、[执行治理合同](../../docs/execution-governance-contract.md)和[阶段4路线](../../PROJECT_PLAN.md#9-阶段-4上下文意图工作与执行门)。

验证要让用户区分“AI认为”“用户接受”“系统正在做”和“已经有证据完成”，并覆盖多Intent、拒绝候选、修改计划、责任变更和模型失败不写假状态。
