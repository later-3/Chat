{
  "extends": "../../tsconfig.base.json",
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"],
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"],
    // 与tsconfig.json相同的并行期临时措施，见tsconfig.json的TODO(B6)。
    "typeRoots": ["../../apps/api/node_modules/@types"]
  }
}
